'use strict';

// ─── 버전 ─────────────────────────────────────────────────────────────────────
// 여기 하나만 고친다. 게임(constants.js)과 서비스 워커(sw.js)가 같은 값을 읽는다.
//
// 예전에는 게임 버전과 SW 캐시 키를 따로 적었다. v0.8.0에서 PWA를 붙인 뒤
// 게임만 v0.9.3까지 올라가는 동안 캐시 키는 'dual-frontier-v0.8.0'에 멈춰 있었고,
// SW의 activate는 "키가 다른 캐시만" 지우므로 아무것도 지워지지 않았다 —
// 한 번 설치한 사람은 무엇을 머지하든 영영 v0.8.0을 보고 있었다.
// 두 군데에 적는 값은 반드시 어긋난다. 그래서 한 군데로 합쳤다.
var GAME_VERSION = 'v0.14.1';
if (typeof self !== 'undefined') self.GAME_VERSION = GAME_VERSION;
