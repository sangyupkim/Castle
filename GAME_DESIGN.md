> **기획서는 [`docs/기획서.html`](docs/기획서.html)를 먼저 보세요.**
> 이 문서는 시스템별 변경 이력과 세부 스펙을 담은 참고 자료이고,
> 기획 판단(설계 원칙 · 미해결 과제 · 로드맵)은 기획서에 정리돼 있습니다.

# 듀얼 프론티어 — 게임 디자인 문서 v12.8

> 마지막 업데이트: 2026-08-29
> 구현 브랜치: `claude/game-analysis-enhancement-j97kyf`

> **⚠️ v9.0에서 하단 전투 모델과 게임 진입 흐름이 통째로 바뀌었습니다.**
> 아래 문서 중 하단 턴제 전투(1초 1틱 · 그룹 순환 · 전투 로그)를 설명하는 대목은
> 더 이상 구현과 맞지 않습니다. 정확한 현행 사양은 `docs/기획서.html`을 보세요.

## v12.8 — 화폐를 가르고 · 상단을 조이고 · 케이브를 건물로

게임 버전 **v0.12.0**. 플레이 보고 여덟 건을 한 판에 반영했다.

### 1. 🎲 강화 카드가 잘려 보이던 것 — 가로 스크롤

🎲풍요(5장) 층이면 414px 자리에 698px을 그려서 양 끝 카드가 화면 밖으로 잘렸다.
**무엇이 있는지도 모르고 고를 수 없는 카드**가 생겼던 것이다. 넘칠 때만 가로
스크롤을 붙이고(양 끝 그늘 · ‹› 화살표 · 진행 막대), 넘치지 않으면 예전처럼 가운데 정렬.

곁에서 하나 더 나왔다 — **리롤하면 5장이 3장으로 줄었다**. `rollUpgradeCards`를
장수 없이 불러 기본값 3으로 다시 뽑고 있었다. 돈을 내고 이벤트 혜택을 잃는 셈이었다.

### 2. 🗿 몬스터 케이브 — 버튼 하나에서 건물로

기능은 있었지만 **건물이 아니었다.** 마을 카드에 붙은 버튼 하나로 5칸짜리 사다리
(CAVE_LEVELS)를 오르는 것이 전부라, 들어가 볼 화면도 고를 것도 없었다.
이제 짓고, 들어가고, 트랙을 고른다.

| 트랙 | 여는 레벨 | 하는 일 |
|---|---|---|
| 💰 전리품 회수 | 0 | 아레나 처치 골드 + |
| ⛏️ 갱도 심화 | 0 | **몹 스탯 +v · 보상 +1.6v** — 위험을 사서 보상을 얻는다 |
| ⚔️ 정예 소굴 | 1 | 정예 등장 확률 + (상한 55%) |
| 💎 정예 전리품 | 2 | 소환 정예 보석 보상 + |
| 🎁 부산물 회수 | 3 | 특수 드랍 확률 + |
| 🪢 길들이기 | 4 | 몹 공격력 − — ⛏️로 산 위험만 도로 깎는다 |
| 🏹 사냥 허가증 | 5 | 정예 소환 기회 +1회 |
| 🌑 심연의 소굴 | 9 | 무한 트랙 |

`gs.caveLevel`이라는 별도 카운터를 없애고 건물 레벨에서 파생시켰다(`caveLevelOf`).
짓지 않았으면 0이다 — 예전에는 새 판에서도 최소 1이라 **한 층도 못 넘긴 판이
이 항 하나로 보석 1개를 받아 갔다.**

### 3. ⏱ 시간이 끝나도 몹이 계속 나오던 버그 (53층 보고)

간격에만 하한(`SPAWN_MIN_GAP` 0.28초)을 걸어 두었다. 마릿수가 창보다 많아지면
간격이 더 못 줄어드니 **일정 자체가 뒤로 늘어났다.**

| 층 | 스폰 일정 끝 | 웨이브 길이 |
|---|---|---|
| 71층 | 78.7초 | 60초 |
| 91층 | **117.9초** | 60초 |
| 100층 | 94.7초 | 60초 |

웨이브는 큐가 빌 때까지 끝나지 않으므로, 타이머가 0이 된 뒤에도 몹이 계속 걸어
나왔다. 이제 창의 길이를 고정하고 **한 번에 여러 마리씩(batch) 내보낸다.**
압력은 그대로 늘면서 웨이브의 길이는 약속대로 남는다 — 전 층 스폰 끝 ≤ 55.2초.

### 4. 상단 난이도 — 도착 간격을 층에 따라 좁힌다

1.3초를 끝까지 유지하면 층이 깊어져도 **같은 밀도**로만 온다. 적이 세지긴 해도
손이 바빠지지는 않아 "타워를 다 세우고 나면 남는 건 구경"이었다.
45층에 걸쳐 1.3초 → 0.70초로 좁힌다. 마리당 보상은 `rewardMult`가 자동으로 낮추므로
골드 총량은 그대로다.

### 5. 🖱 PC에서 스크롤이 안 되던 것

캔버스가 480×800이라 **넓은 화면에서는 가운데 좁은 기둥**으로 놓인다(1600×900이면
x 530~1070). 휠 리스너가 캔버스에만 걸려 있어서, 커서가 양옆 여백에 있으면 휠이
아무 데도 닿지 않았다. 창 전체에서 받고 캔버스 밖 좌표는 기둥 안으로 끌어당긴다.
PageUp/Down · Home/End도 붙였다.

### 6. ×10 강화 버튼

트랙 하나가 24~30단, 스킬 노드가 10레벨인데 한 칸씩만 살 수 있었다.
**지금 지갑으로 실제 몇 칸 살 수 있는지와 그 총액을 버튼에 미리 적는다** —
`×7 / 1,240💰`처럼. 눌러 보고 알게 되는 값이 아니어야 한다.

### 7. 💎와 💰의 뜻을 하나로

보고: *"대장간을 골드로 해줘. 보석은 무조건 영구적, 캠프에서만. 골드는 무조건 스테이지 중에만."*

| 화폐 | 어디서 | 무엇이 남나 |
|---|---|---|
| 💰 골드 | 판 안에서 벌고 판 안에서 쓴다 | 판이 끝나면 사라진다 |
| 💎 보석 | 캠프에서만 쓴다 | 영원히 남는다 |

⚒️ 대장간(연마·합성·담금질)이 유일한 예외였다 — 판 안의 건물이 영구 화폐를
요구했다. 전부 골드로 옮기고 상태도 `gs.town.forge`로 내려 판마다 초기화된다.

### 8. 🔥 캠프 단련소 — 끝이 없고 확정이 아닌 사용처

보고: *"3트 정도에 100층을 돌파했는데 보석이 남아돌아. 캠프 업글 소모량을 대폭 늘리고
스킬트리 업글량도 늘려줘."* 그리고 *"대장간에 있는 확률강화를 캠프에 만들어줘.
실패 시 파괴도 되고 단계를 낮추기도 하게."*

실제로 세어 보니 100층 한 번 돌파 = **9,006보석**, 캠프에서 살 수 있는 것 전부 =
**6,934보석**. 첫 돌파에서 이미 모든 것이 끝났다.

- 스킬 노드 값을 `기본×L`에서 `기본×L×(1+0.35(L-1))`로 — 노드 총액 ×55 → ×170.5,
  **트리 전체 4,236 → 13,030**
- 해금 **86 → 322** (초반 진행축이라 폭은 작게)
- 새 **🔥단련** 탭 — 보석을 걸고 굴린다. 성공 +1 / 실패는 유지·하락·파괴 세 갈래.
  5단마다 안전지대가 있어 무너져도 거기까지만 떨어진다. 4항목 × 40단,
  전부 성공해도 132,720보석이고 실패를 감안하면 그 서너 배다.
- 처치 보석을 60마리당 1 → **100마리당 1**. ③④에서 물량을 절반 넘게 늘렸는데
  그대로 두면 **난이도를 올린 만큼 보석도 불어난다** — 어렵게 만든 값이 보상으로
  되돌아오면 바꾼 것이 없다.

### 9. 근접이 다 죽고 활만 남던 것

몹 공격력은 `sm^0.80`으로 오르는데 아군 체력은 정액 합산이라 곡선을 못 따라간다.
그 차이를 **접촉하는 쪽**이 전부 뒤집어쓴다.

| 유닛 | HP | ATK | DEF |
|---|---|---|---|
| ⚔️ 검사 | 105 → **152** | 12 → 10 | 3 → 5 |
| 🛡️ 방패병 | 210 → **305** | 7 → 6 | 9 → 12 |
| 🗡️ 도적 | 82 → **106** | 14 → 12 | 1 → 2 |
| 🪓 광전사 | 130 → **176** | 21 → 18 | 2 → 4 |
| ✝️ 성기사 | 265 → **445** | 13 → 10 | 13 → 19 |
| 🏹 궁수 | 70 → **52** | — | — |
| 🎯 명사수 | 78 → **58** | — | — |

근접은 버티는 것이 일이므로 체력·방어를 올리고 공격을 내렸다. 활은 안 맞는 값을
이미 사거리로 받고 있으니 체력을 내렸다. ✝️성기사는 '방패병보다 단단하다'가
이름값이라 폭을 가장 크게 줬다.

---

## v12.7 — 🧭 손가락으로 짚는 첫걸음

게임 버전 **v0.11.3**. 플레이 보고: *"가장 처음 게임에 들어갔을 때 친절한 튜토리얼 기능을
좀 만들고 싶어. 훈련모드 시작 버튼을 누르라는 것, 들어가서 타워를 배치하라는 튜토리얼,
그다음에 영웅을 하단에 배치하라는 튜토리얼, 그런 식으로 가장 기초적인 게임 방식을
알려주는 튜토리얼이 필요한데."*

기존 튜토리얼(`tutorial.js`)은 **글 여섯 장**이었다. "이 게임이 무엇인가"는 말해 주지만
**손이 어디로 가야 하는지**는 말해 주지 않는다. 여섯 장을 넘기고 나면 버튼이 열몇 개인
캠프 한복판에 혼자 남는다. 그래서 글 대신 **손가락**을 만들었다 (`js/guide.js`).

### 네 가지 원칙

| 원칙 | 이유 |
|---|---|
| 눌러야 할 버튼을 화면에서 **직접 짚는다** | 그 자리만 밝히고 나머지는 62% 어둡게 |
| **탭을 가로채지 않는다** | 진짜 버튼을 진짜로 누른다. 안내가 먹는 탭은 [안내 끄기] 하나뿐 |
| **확인 버튼이 없다** | "다음"을 누르게 하면 *안내를 넘기는 것*이 과제가 된다. 시킨 일을 해낸 것이 곧 다음 장 |
| 화면이 어긋나면 **되돌아갈 길을 짚는다** | 감추지 않는다 (아래) |

### 감추는 대신 되돌아가는 길을 짚는다

처음에는 화면이 어긋나면 안내를 그냥 감췄다. 엉뚱한 곳을 짚지 않으려는 뜻이었는데,
검증해 보니 정반대로 굴렀다 — 마을에 들어가 🏰마을 탭에 서 있는 초보에게는
"병력을 고용하세요"가 보이지 않고 화면만 캄캄해진다. **도와주려던 안내가 제일 헤매는
순간에 사라지는** 셈이다. 그래서 우회 안내(청록색)를 만들었다.

| 상황 | 짚는 것 |
|---|---|
| 필요한 탭이 아니다 | 그 탭 버튼 — "다음 할 일 — 타워부터 한 기" |
| 건물 상세 화면에 들어가 있다 | 나가는 탭 버튼 (탭을 누르면 화면도 같이 닫힌다) |
| 마을에서 할 일인데 전투 화면이다 | [🏰 마을] 버튼 |
| 짚을 것이 스크롤 밖이다 | "▼ 화면을 위로 밀어 …" |

안내가 사라지는 경우는 이제 딱 하나 — 다른 페이지에 있어서 짚을 것이 이 화면에
정말 없을 때뿐이다.

### 순서는 골드가 정했다

처음에는 **고용 → 영웅 → 타워** 순서로 짰다. 실제로 돌려 보니 3단계에서 **골드 4**로
멈췄다. 시작 골드는 10인데 제일 싼 궁수가 6, 화살탑이 5라 **둘 다 사는 것이 애초에
불가능**했다. 안내를 따라간 사람이 안내를 끝낼 수 없었던 것이다.

첫 웨이브를 세 조합으로 실측했다:

| 10골드로 할 수 있는 것 | 영웅 | 성벽 |
|---|---|---|
| 경로 위 타워 1기 · 영웅 혼자 | **전사** | 83/100 |
| 구석(0,0) 타워 1기 · 영웅 혼자 | **전사** | 66/100 |
| 타워 0기 · 궁수 1명 · 영웅 | **전사** | 68/100 |
| *(참고)* 경로 타워 + 궁수 — 10골드로는 불가능 | 생존 HP31 | 100/100 |

**어느 쪽을 골라도 영웅은 첫 웨이브에 쓰러진다.** 곁에 한 명만 세워도 살아남는다는 것이
네 번째 줄이다. 그러면 숨길 것이 아니라 가르칠 것이다 — *혼자 버티는 것이 한계*라는 사실
자체가 "왜 병력을 고용하는가"의 답이기 때문이다.

그래서 성벽이 가장 덜 깎이는 **타워 먼저**로 두고, 고용을 첫 웨이브 뒤로 옮겼다:

```
① 훈련 시작 → ② 마을로 → ③ 타워 한 기(5골드) → ④ 영웅 하단 배치
→ ⑤ 출전 → ⑥ 전투 관전 → ⑦ 병력 고용 (한 웨이브 벌고 나서)
```

전투 안내는 "쓰러져도 한 층 쉬었다 돌아오니 괜찮습니다"라고 미리 말해 두고,
고용 안내의 제목이 **"혼자로는 벅찹니다"**다.

### 타워 단계는 두 박자

자리를 집기 전에는 격자를, 집고 나면 **[여기에 세운다]·[✕취소]를 함께** 짚는다.
계속 격자만 짚었더니 말풍선이 확인 버튼을 그대로 덮어 — *누르라고 한 버튼을 안내가
가리는* 꼴이 됐다.

### 구멍이 구멍이 아니었다

덮개에 구멍을 뚫을 때 `ctx.rect(0,0,CW,CH)` 위에 `roundRect()`를 쌓고 `fill('evenodd')`를
불렀는데, **`roundRect()`는 안에서 `beginPath()`를 부른다**. 방금 쌓은 전체 화면 사각형이
지워지면서, 짚은 자리에 구멍이 나는 대신 **그 자리만 불투명하게 덮였다** — 안내가
"이 버튼을 누르세요"라며 그 버튼을 칠하고 있었다. 헬퍼를 쓰지 않고 경로를 직접 이어 붙였다.

짚을 곳이 없는 단계(전투 관전)는 덮개를 22%로 낮췄다. 55%로 덮었더니
"두 전선이 함께 굴러가는 걸 보라"면서 그 전선을 가리고 있었다.

### 다시 보는 길

글 설명에 [📖 처음부터 다시]가 있듯 손가락 안내에도 [🧭 첫걸음 안내]를 뒀다
(🏆기록 탭 → 📖 게임 안내). 처음에는 그 줄 밑에 한 줄을 더 달았는데, 새 줄이 로비 하단
**고정 출격 바(y749~) 밑으로 들어가 스크롤로도 닿지 않았다.** 줄을 늘리는 대신
한 줄에 셋을 놓았다.

### 검증

신규 플레이어로 처음부터 끝까지 안내만 따라가 7단계를 모두 통과(골드 10 → 타워 5 →
한 웨이브 59 → 궁수 고용), `df_guide_v1` 기록 후 재방문 시 미표시.
페이지 3종 × 마을 탭 3종 × 단계 9까지 전수 조회에서 예외 0.
[🧭 첫걸음 안내]로 다시 켜면 기록이 지워지고 1단계부터 다시 짚는다.

---

## v12.6 — ◎ 사거리를 보고 놓는다

게임 버전 **v0.11.2**. 플레이 보고: *"타워 배치에서 각 타워의 사거리를 볼 수 있으면 좋겠어.
설치되기 전에도 볼 수 있으면 좋겠는데, 그럼 선택하고 배치하고 배치 확인까지 하는 단계가
있어야 할 것 같은데."*

예전에는 빈 칸을 누르면 **그 자리에서 바로 세워졌다**. 어디까지 닿는지는 세워 본 뒤에야
알 수 있었고, 잘못 놓으면 이설비를 물거나 60% 손해를 보고 팔아야 했다.
되돌리는 값이 비싼 결정을 되돌아볼 자리 없이 하고 있었던 것이다.

### 세 단계로 나눴다

| 단계 | 무엇이 보이나 |
|---|---|
| ① 고른다 | 팔레트 카드마다 **◎사거리 칸수** (화살 2.4 · 저격 5.0 · 번개 2.6 …) |
| ② 놓아 본다 | 빈 칸을 탭하면 그 자리에 **점선 사거리 원 + 십자 표적**. 아직 안 세워진다 |
| ③ 확인한다 | ATK · 공속 · 사거리 · 값, 그리고 **경로 N칸 사정권** · [✔ 여기에 세운다] [✕ 취소] |

- 다른 칸을 탭하면 미리보기가 그리로 옮겨간다.
- **같은 칸을 다시 탭하면 곧바로 확정** — 두 번 탭이 '놓고 확인'이 된다.
- 종류를 바꾸면 집어둔 자리를 그 종류로 다시 본다 (사거리가 종류마다 다르다).
- 골드가 모자라 실패하면 자리를 **붙들어 둔다** — 다시 집게 만들 이유가 없다.

이미 세운 타워를 탭해도 사거리 원이 뜬다. 배치를 다시 볼 때도 같은 눈이 필요하다.

### 원보다 정직한 숫자

사거리 원만 보여주면 **"커 보이는데 정작 길을 안 덮는"** 자리를 못 거른다.
그래서 `pathCellsInRange()`로 그 자리가 경로를 몇 칸 사정권에 넣는지 세어 같이 띄운다.
0칸이면 빨갛게 **"경로에 닿지 않습니다"**.

### 좌표

미니 그리드는 실제 격자를 `mg.cellW / CELL_W`만큼 줄여 그린 것이라, 사거리도 같은 비율로
줄여야 "이 칸에서 저기까지 닿는다"가 눈으로 맞는다. `scale`(0.62)이 아니라
**실제로 그려진 칸 폭의 비율**을 쓴다 — `floor(53×0.62)/53 = 0.604`로 다르다.

그리고 원을 격자 사각형으로 잘라낸다. 저격탑은 5칸이라 안 자르면 원이 위쪽 팔레트 카드까지 덮는다.

---

## v12.5 — 패널 121곳 전면 적용

게임 버전 **v0.11.1**. 플레이 보고: *"할 수 있는 한 최대한 적용해봐."*

이 게임의 패널은 전부 `roundRect + 채움 + 테두리` 한 덩어리였다 — **121곳**.
그걸 `uiPanel()` 하나로 모으고, 그 안에서 팩의 9슬라이스 틀을 두른다.

### 크기로 가른다

팩 틀에는 모서리 장식이 있다. 작은 배지·칩까지 두르면 한 화면에 장식이 쉰 개씩 깔려
도리어 지저분해진다. **큰 담는 것은 틀을, 작은 것은 지금처럼 매끈한 둥근 사각형을.**

| | 처리 |
|---|---|
| 60×30 이상 | 9슬라이스 틀 + 원래 채움색 50% + 원래 테두리색을 안쪽에 가늘게 |
| 그 미만 | 예전 `roundRect` 그대로 |

크기는 **실행 중에** 본다. 그래서 호출부는 전부 `uiPanel(...)` 한 모양으로 통일되고,
어떤 옷을 입힐지는 함수가 정한다.

### 원래 색을 버리지 않는 이유

섹션마다 다른 강조색(보라=부적 · 파랑=검사 · 초록=치유사 · 주황=마법사)이
이 게임의 **정보 구조**다. 틀로 통째로 갈아치우면 그 구조가 사라진다.
그래서 채움은 50%로 틀 위에 얹고, 테두리색은 안쪽에 1.2px로 남긴다 —
**틀은 팩에서, 뜻은 색에서**. `drawBtn`에서 세운 원칙을 패널까지 밀고 나간 것이다.

### 변환하다 한 번 깨뜨렸다

정규식의 조각을 `(.+?)`로 잡았더니 `re.S` 아래에서 **문장을 넘어** 매칭돼
`uiPanel(ctx, ..., 'rgba(8,4,6,0.86)'; ctx.fill();` 같은 것이 나왔다.
조각이 세미콜론을 넘지 못하게(`[^;]+?`) 막고, 인자가 정확히 다섯 개인지
괄호 깊이를 세어 확인한 뒤에야 121곳이 하나도 빠짐없이 변환됐다.

---

## v12.4 — 분기 18갈래에 18명 · Cryo's Mini GUI

게임 버전 **v0.11.0**. 플레이 보고: *"Cryo's Mini GUI 폴더를 업로드해왔어. UI 관련 에셋인데
이것도 적용하고, NPC 팩에 26종 있다고 한 것도 같이 적용해서 머지해."*

### 👷 분기 18갈래에 각각 다른 얼굴

두 겹으로 가른 원래 이유가 이것이었다. 석대는 그대로 두고 **사람만 갈아 끼운다** —
통짜 그림이었다면 18장을 새로 그려야 했을 일이 매니페스트 줄 추가로 끝난다.

| 타워 | 분기 1 | 분기 2 | 분기 3 |
|---|---|---|---|
| 🏹 화살탑 | 💨 속사 = 도둑 | 🎯 관통 = 궁수 | 🏹 공성 = 대형 기사 |
| ❄️ 서리탑 | 🧊 혹한 = 키 큰 수녀 | 💎 서릿발 = 마법사 | 🌨️ 눈보라 = 수녀 |
| 💣 대포탑 | 💥 융단 = 백정 | 🛡️ 철갑탄 = 중장 기사 | 🚀 박격 = 대장장이 |
| 🎯 저격탑 | 🎯 헤드샷 = 처형인 | 🔫 연사 = 전령 | 💀 대물 = 정예 기사 |
| ⚡ 번개탑 | ⛓️ 연쇄 = 마법 상인 | 🔋 과충전 = 왕 | 🛩️ 대공 = 공주 |
| ☠️ 독탑 | ☠️ 맹독 = 연금술사 | 🌫️ 확산 = 뚱뚱한 수녀 | 🧪 부식 = 상인 |

종류 기본값 6명까지 합쳐 **24명 × 4프레임 = 96장**. 칸마다 `col·row`로 위상을
어긋나게 해서 마흔 기가 한 박자로 흔들리지 않게 했다.

### 🖼 Cryo's Mini GUI

팩은 작다(12장). 아틀라스에서 쓸 조각만 잘라 냈다.

| 조각 | 쓰임 |
|---|---|
| 9슬라이스 판 5종 (dark · gold · navy · paper · wood) | 버튼 틀 · 강화 카드 틀 |
| 막대 채움 4색 (red · blue · green · orange) | HP · MP 막대 |

`steel`·`stone` 두 종은 아틀라스에서 크롭이 어긋나 모서리가 뭉개져 버렸다 — 뺐다.

**적용한 곳 — 한 함수를 고쳐 게임 전체에 닿는 자리를 골랐다.**

- `drawBtn()` — 게임의 거의 모든 버튼이 이 함수 하나를 지난다. 팩 틀을 깔고
  **원래 색을 그 위에 42%로 얹는다**. 색을 통째로 그림에 넘기면
  초록=준비 / 빨강=위험 같은 신호가 사라진다 — **틀은 팩에서, 뜻은 색에서** 가져온다.
- `drawHPBar()` / `drawMPBar()` — 기지·보스·용병·영웅의 모든 체력 막대가 이 둘을 지난다.
  위급함을 색으로 알리던 규칙(>50% 초록 / >25% 주황 / 그 이하 빨강)은 그대로 두고
  칠만 팩 그림으로 바꿨다.
- 강화 카드 — 매 층 멈춰 서서 고르는 화면이라 여기만큼은 진짜 틀을 둘렀다.
  일반은 `dark`, 희귀·영웅은 `gold` 틀에 등급 색을 얹는다.

`drawPanel9()`는 모서리 8px을 고정하고 변과 가운데만 늘린다 — 통째로 늘리면
테두리 장식이 뭉개진다. 그림이 없으면 `false`를 돌려주고 부르는 쪽이
예전 `roundRect`로 물러난다. 매니페스트를 비워도 게임이 돌아가야 한다는 원칙 그대로다.

---

## v12.3 — 석대 + 점유자로 확정

게임 버전 **v0.10.3**. 플레이 보고: *"석대로 한 걸로 최종결정."*

플래그(`df_tower_art`)를 걷어내고 **석대 + 점유자를 타워의 유일한 그림 방식**으로 확정했다.
비교용으로 남겨 뒀던 옛 도형 받침대(`drawTowerPedestal`, 60여 줄)도 함께 지웠다.

### 물러나는 순서

플래그를 지운다고 그림이 없을 때 화면이 비면 안 된다. 세 단계로 물러난다.

1. `crew.<종류>` + `keep.*` — **확정된 방식**. 석재 타일을 쌓고 그 위에 사람을 세운다.
2. 점유자 그림이 없으면 — 옛 통짜 타워 그림(`tower.<종류>.1`)을 맨땅에 세운다.
3. 그것도 없으면 — 색 사각형 + 이모지.

2번이 남아 있는 이유는 매니페스트를 비우고 돌려도 게임이 돌아가야 하기 때문이다.
`SPRITE_FILES`를 통째로 지우면 이모지 판으로 되돌아간다는 원칙은 이 프로젝트가
처음부터 지켜 온 것이라, 확정과 무관하게 유지한다.

### 남은 자리

점유자는 아직 **타워 종류별**로만 다르다. ★5 분기 18갈래에 각각 다른 얼굴을 주는 것이
두 겹으로 가른 원래 이유였고, NPC 팩에 26종이 있으니 매니페스트에 줄만 더하면 된다.
지금은 분기를 **깃발 색**으로만 구분한다.

---

## v12.2 — 석대를 진짜 돌로 · 용병·영웅·몹 전부 그림으로

게임 버전 **v0.10.2**. 플레이 보고: *"석대가 도형이라는 건 이모지처럼 만든 거라는 거야?
진짜 석재 타일이 있으면 그걸로 갈아끼워줘. 몬스터 후보랑 타워 점유자랑 지금 용병들이랑
영웅 이미지도 바꿔서 적용된 거야?"*

### 먼저, 정확히 답하면

**"도형"은 이모지가 아니라 캔버스 명령이었다.** `fillRect`·`arc`로 코드가 직접 그린
회색 사각형이다. 그림 파일이 아니라서 팔레트가 옆 픽셀 아트와 따로 놀았다.

**그리고 용병·영웅·몹은 하나도 적용돼 있지 않았다.** 지난 판에서 붙인 것은
독탑 그림 · 마왕 그림 · 타워 점유자(그마저도 플래그 뒤) 셋뿐이고,
몬스터는 **후보만 보여드린 것**이었다.

### 🧱 석대 — 0x72 석재 타일로 교체

| | 이전 | 이후 |
|---|---|---|
| 몸통 | `fillRect` 회색 사다리꼴 | `wall_mid` 벽돌 타일을 격자로 |
| 처마 | 회색 막대 | `wall_top_left/mid/right` |
| 출입구 | `arc`로 그린 아치 | `doors_leaf_closed` 나무문 |
| 총안 | 사각형 네 개 | `wall_hole_1` |
| ★5 표시 | 난간 색칠 | **`wall_banner_*` 깃발** — 분기 색에 가장 가까운 것을 건다 |

깃발은 넷뿐이고 분기는 열여덟이라 `bannerKeyFor()`가 RGB 거리로 가장 가까운 것을 고른다.

### ⚔️👑👾 훅은 있었는데 그림이 없었다

`arenaSpriteKey`는 예전부터 `unit.<타입>` · `hero.<각인>` · `mob.<타입>`을 찾고 있었다.
**매니페스트에 그 키가 하나도 없어서** 스무 종류가 전부 '색깔 원 + 이모지'로 그려졌다.

| 용병 | 그림 | | 영웅 각인 | 그림 |
|---|---|---|---|---|
| 검사 | Knight | | ⚔️ 검성 | Heavy Knight |
| 궁수 | Archer | | 🛡️ 수호자 | Large Elite Knight |
| 치유사 | Bishop | | 🔥 술사 | Magic Shopkeeper |
| 방패병 | Large Knight | | | |
| 마법사 | Mage | | **아레나 몹** | |
| 도적 | Thief | | 고블린 | goblin |
| 광전사 | Butcher | | 사냥개 | wogol |
| 성기사 | Elite Knight | | 오크 | orc_warrior |
| 명사수 | Herald | | 암흑궁수 | chort |
| | | | 오우거 | ogre |
| | | | 보스 | big_zombie |
| | | | 군주 | masked_orc |

**전부 4프레임 걷기로 넣었다.** 한 장짜리로 두면 실시간 아레나에서 죽은 것처럼 보인다.
개체마다 `e.id`로 위상을 어긋나게 해 스무 마리가 한 박자로 움직이는 것도 막았다.

### 곁다리로 고친 것

**그림 비율이 눌리고 있었다.** 아레나는 가로·세로에 각각 고정 배수를 곱했는데
(`r×2.1`, `r×2.6`), 새 그림은 16×16(고블린)부터 32×36(오우거)까지 비율이 제각각이라
정사각 그림은 늘어나고 세로긴 그림은 눌렸다. 가로를 기준으로 잡고 세로는
**원본 비율을 따라가게** 고쳤다.

---

## v12.1 — 새 에셋 팩 두 개 · 타워를 두 겹으로 가르다

게임 버전 **v0.10.1**. 플레이 보고: *"Fantasy RPG NPCs랑 0x72_DungeonTilesetII를
통째로 넣어놨거든. 보고 쓸만한 거 있으면 가져다 써봐. 그리고 타워 에셋이 부족한데
타워는 기본 타워 위에 사람 에셋을 올려서 쓰는 방식으로 해볼까."*

### 바로 메운 구멍 둘

**독탑에 그림이 없었다.** `spr_tower_poison_wizard.png`는 처음부터 Simple TD 팩에
들어 있었는데 매니페스트 배선이 빠져 있었다. 여섯 타워 중 독탑 하나만 이모지로
그려지고 있었다. 투사체도 같이 붙였다.

**👹 마왕에 그림이 없었다.** 0x72 팩의 `big_demon`(32×36, idle/run 4프레임)이
그대로 맞았다. 100층에 한 마리만 나오므로 배우로 등록하지 않고 낱장 8프레임을
시간으로 돌린다.

### 🧱 받침대 — 레벨이 처음으로 보인다

`towerSpriteKey`는 레벨을 3티어로 묶어 `tower.<종류>.<티어>`를 찾는데,
매니페스트에 **`.1`밖에 없었다**. 즉 ★1과 ★10이 완전히 같은 그림이었다.

그림을 티어마다 새로 그리는 대신 **아래를 쌓는다**. 받침대는 도형이라 공짜로 늘어난다.

| 레벨 | 받침대 |
|---|---|
| ★1~2 | 없음 — 맨땅에 선 초소 |
| ★3~4 | 1단 석대 |
| ★5~ | 2단 석대 + 총안 · **★5 분기를 고르면 그 색으로 물든다** |

### 👷 석대 + 점유자 — 통짜를 두 겹으로

제안하신 방식을 그대로 구현했다. 지금 타워 그림은 '건물 + 그 위의 사람'이 한 장에
그려진 **통짜**라, 종류마다 한 장씩 필요하다. 두 겹으로 가르면 조합이 곱셈이 된다.

```
통짜:  6종 × 3티어 × 3분기 = 54장을 그려야 한다
두 겹: 석대 3티어 + 사람 26종 = 29장으로 78가지
```

특히 ★5 분기 **18갈래에 각각 다른 얼굴**을 주려면 통짜로는 18장을 새로 그려야 하는데,
두 겹이면 사람만 갈아 끼우면 된다.

| 타워 | 점유자 | 이유 |
|---|---|---|
| 🏹 화살탑 | Archer | 그대로 |
| ❄️ 서리탑 | Mage | 마법 계열 |
| 💣 대포탑 | Blacksmith | 망치 = 화포 |
| 🎯 저격탑 | Executioner | 두건 쓴 처형인 |
| ⚡ 번개탑 | Magic Shopkeeper | 지팡이 든 술사 |
| ☠️ 독탑 | Alchemist | 약병 |

석대 색은 **기존 타워 그림에서 뽑았다**(`#657392` 몸통 / `#2a2f4e` 그늘) —
팔레트가 어긋나면 사람만 새 팩이고 석대만 도형인 티가 난다.
벽돌 줄눈을 한 줄 걸러 어긋나게 놓고 아치형 출입구를 넣어 '건물'로 읽히게 했다.

**기본값은 기존 방식(`sprite`)으로 두었다.** 게임 전체의 그림체가 바뀌는 결정이라
한쪽을 골라 밀어붙이지 않고, `localStorage`의 `df_tower_art`를 `'crew'`로 바꾸면
바로 갈아 끼워 비교할 수 있게 했다.

- **sprite** — 픽셀 아트 타워가 더 정교하고 타일과 붙는다. 대신 여섯 종류가 다 비슷해 보인다.
- **crew** — 궁수·마법사·대장장이가 한눈에 갈린다. 대신 석대가 도형이라 손맛이 덜하다.

### 아직 안 쓴 것

0x72 팩에는 몬스터가 20종 가까이 들어 있다(`skelet` `chort` `necromancer` `ogre`
`orc_shaman` `imp` `wogol` `swampy` …). 지금은 배우 넷(slime/orc/wolf/bee)을
색만 바꿔 열넷에 돌려 쓰고 있으므로, 여기가 다음으로 값이 큰 자리다.

---

## v12.0 — 결승선을 그었다 · 🌑 악몽 1~10단계 (기획서 v5.0)

게임 버전 **v0.10.0**. 플레이 보고: *"게임에 목표가 없으면 지루해지기 마련이잖아.
지금 마련된 스테이지를 100스테이지까지가 목표로 하고, 거기서 서약 이벤트들을 하나씩 붙인
난이도를 악몽 1단계 이런 식으로 이름을 붙여서 목표점을 만드는 건 어떨까."*

맞는 지적이었다. 심연은 "죽어야 끝나는" 무한이었고, 끝이 없다는 건 **목표도 없다**는
뜻이다. 최고 21층을 찍은 사람에게 다음 목표가 "22층"이면 그건 목표가 아니라 눈금이다.

### 새 진행 구조

```
⚔️ 훈련 30웨이브
   ↓
∞ 심연 1~100층 → 👹 마왕
   ↓
🌑 악몽 1단계 (서약 1개) → 마왕
🌑 악몽 2단계 (서약 2개) → 마왕
   …
🌑 악몽 10단계 (서약 10개) → 마왕
   ↓
♾️ 무한 — 결승선 없음
```

100층 × 11갈래(심연 + 악몽 10) = **1,100층**. 같은 층이라도 서약 조합이 다르면 다른 판이 된다.

### 사다리에 서약을 쓴 이유

서약은 이미 열 개를 만들어 뒀다. 악몽 단계마다 하나씩 쌓으면 **새 콘텐츠를 만들지 않고
난이도 열 단계**가 생긴다. 순서는 '약한 것부터'가 아니라 **성격이 번갈아 오도록** 짰다 —
적을 세게 하는 서약만 연달아 붙으면 3단계쯤에서 화력만 올리면 되는 문제가 되고,
나를 약하게 하는 것만 붙으면 무엇을 해도 안 되는 벽이 된다.

| 단계 | 추가되는 서약 | 갈래 |
|---|---|---|
| 1 | 💨 질주 본능 — 적 이속 +35% | 적 |
| 2 | 😴 짧은 휴식 — 웨이브 후 회복 없음 | 경제 |
| 3 | 🛡️ 무쇠 가죽 — 적 방어 +6 · 타워 −15% | 적 |
| 4 | ⏸️ 멈춘 시간 — 과부하 사용 불가 | 나 |
| 5 | 💢 거친 침입자 — 적 HP ×1.8 | 적 |
| 6 | 👛 빈 주머니 — 골드 −40% | 경제 |
| 7 | 🕯️ 고독한 지휘 — 슬롯 절반 · 아군 HP −25% | 나 |
| 8 | 🌑 굶주린 무리 — 스폰 −30% · 몹 HP ×1.5 | 적 |
| 9 | 📐 봉인된 설계도 — 타워 레벨 3 제한 | 나 |
| 10 | 🧱 얇은 성벽 — 기지 HP 절반 · 수리 없음 | 나 |

누적이다 — 악몽 10단계는 열 개가 한꺼번에 걸린다. 캠프에서 뺄 수 없고, 그 위에 더 걸 수는 있다.
보석은 단계당 +35%(악몽 10 = **×4.5**), 클리어 시 일회성으로 `40 + 단계×25`개.

### 👹 마왕 — 100층의 끝

100층은 '조금 더 센 층'이 아니라 **끝**이어야 한다. 그래서 잡몹이 하나도 없고 마왕만 온다.

| | 값 |
|---|---|
| 체력 | **그 층 적 전체 총량의 85%** (심연 기준 실측 약 6.7억) |
| 악몽 배수 | 단계당 ×1.16 (악몽 10 = ×2.6) |
| 이동 | 층 이동속도 곡선을 **타지 않는다** — 기지까지 약 66초 |
| 페이즈 | 체력 1/3마다 이속 ×1.10 + 호위 12기 소환 (총 2회) |
| 기지 도달 | **그 자리에서 판이 끝난다** (dmg 99999) |

체력을 상수가 아니라 **그 층의 총량에 걸어 뒀다**(`floorTotalHp`). 상수로 박으면
곡선을 다시 잡을 때마다 보스만 혼자 뒤처진다. 처음에 `900 × 곡선`으로 잡았더니
100층 총량의 **1.1%**였고, 만렙 화력으로 **0.1초**에 끝났다.

### 실측 — 마왕전이 성립하는가

만렙(스킬트리 만렙 · 마을 만렙 · ★10 타워 40기) 기준:

| | 처치 시간 | 마왕 도달 |
|---|---|---|
| ∞ 심연 | **14.5초** | 65.9초 (페이즈 가속 후 47.3초) |
| 🌑 악몽 10 | **37.8초** | 47.3초 |

심연의 마왕은 만렙에게 마무리 한 판이고, **악몽 10은 20% 남기고 잡는 진짜 벽**이다.
보스만 보고 짠 편성(💀대물 저격의 처형 · ☄️유성 낙하 · 🧪부식)이 아니면 넘기 어렵다.

처음 잡았을 때 이동속도가 층 곡선을 타서 35초 만에 기지에 닿았다 —
'느리고 거대한 것'이 아니라 돌진하는 것이 됐다. 보스가 걸어오는 시간은 고정이어야
"언제까지 잡아야 하나"가 읽힌다.

### 곁다리로 고친 버그

**전투 중에 [🏰마을] 자리를 누르면 마을로 튕겼다.** `gs.ui`는 그리면서 채워지고
**페이지가 바뀔 때만** 비워지는데, 준비 화면 → 전투는 페이지가 그대로 `battle`이라
아무도 지우지 않았다. 준비 화면의 버튼 좌표가 아레나 위에 그대로 살아 있었던 것이다.
`renderBattle` 첫머리에서 매 프레임 비우고, 준비 화면일 때만 다시 등록하게 했다.

**악몽 서약이 켜진 것으로 보이는데 BONUSES에는 안 붙었다.** `startRun`이
`reapplyAllBonuses`를 부른 **뒤에** `gs.inRun = true`를 켰는데,
강제 서약 판정이 `gs.inRun`을 보고 있었다. 슬롯 버그(v11.19)와 정확히 같은 종류다 —
**계산이 입력보다 먼저 도는 순서 문제**.

---

## v11.19 — 👥증원이 영구 강화가 되고 있었다 (버그)

게임 버전 **v0.9.10**. 플레이 보고: *"무기공방을 업그레이드했는데 갑자기
용병칸이 4칸에서 6칸으로 늘어났어."*

무기공방은 죄가 없었다. `gs.battle.maxSlots`는 계산해서 넣어두는 값인데,
그 입력 중 하나가 **👥증원** 층 이벤트(`fev('slotBonus')` = +2)다.
그런데 재계산 순서가 어긋나 있었다.

```
웨이브 N 종료 → recalcMaxSlots   ← 아직 N층의 이벤트로 계산
wm.init(N+1)  → floorEvent 교체  ← 재계산 없음  ★
준비 화면                         ← 슬롯이 '지난 층'의 값을 들고 있다
무기공방 강화 → reapplyAllBonuses → 그제서야 재계산 → 4칸이 6칸으로 튄다
```

강화가 슬롯을 늘린 것이 아니라, **재계산을 부르는 아무 행동이나** 밀린 값을
드러낸 것이다. 마을에서 무엇을 샀어도 똑같이 튀었다.

같은 원인으로 반대 방향이 더 나빴다.

| | 이전 | 이후 |
|---|---|---|
| 증원 층 진입 → 마을 강화 | 4칸 → **6칸으로 튐** | 진입 시점부터 6칸 · 안 튐 |
| 증원 층을 지나 다음 층 | **6칸 그대로 남음** | 4칸으로 복귀 |
| 증원 층에서 6명 뽑고 다음 층 | **6명 전부 따라감** | 2명 해산 + 고용비 전액 환불 |

세 번째가 실질적인 문제였다 — 👥증원은 "한 층짜리" 이벤트인데,
그 층에서 6명을 뽑으면 **영구 +2 강화**가 되고 있었다.

### 고친 것

1. `wm.init()`에서 층 이벤트를 정한 **바로 그 자리**에서 슬롯을 다시 센다.
   이벤트가 슬롯의 입력이면, 이벤트가 정해지는 곳에서 같이 세야 한다.
   (전체 `reapplyAllBonuses`가 아니라 `recalcMaxSlots`만 — 직전 웨이브 종료 때
   BONUSES는 이미 최신이고, 부수효과를 다시 돌릴 이유가 없다.)
2. `releaseOverCapUnits()` — 슬롯이 줄었는데 이미 더 데리고 있으면 넘치는 만큼
   해산하고 **고용비를 전액 환불**한다. 내가 자른 것이 아니라 층이 끝나서 자리가
   사라진 것이므로 반값이 아니라 전액이다. 나중에 뽑은 쪽부터 보낸다.

### 검증

- 신고 시나리오 그대로: 증원 층 준비화면 6칸 → 무기공방 강화 3회 → **6칸 유지**
- 40개 층을 훑어 증원 층은 전부 6칸, 나머지는 전부 4칸 (`quake+levy` 복합 층 포함)
- 🎴모병 영장 부적(+2) 6칸 · 🕯️고독한 지휘 서약(슬롯 절반) 한도 2 —
  둘 다 오작동 해산 0건
- 해산 로그: `👥 증원이 끝나 용병 2명 해산 +12💰`

---

## v11.18 요약 — ★5 분기 특화 (기획서 v4.18)

게임 버전 **v0.9.9**. 지금까지 타워를 키우는 일은 "같은 것이 커진다"뿐이었다.
Lv.1 화살탑도 Lv.5 화살탑도 소형에 강하고 대형에 약했다 — 레벨은 숫자만 올리고
성격은 하나도 안 바꿨다. 상성표가 있어도 **한 번 지으면 그 타워의 역할은 끝까지 고정**이었다.

★5에 닿으면 세 갈래 중 하나를 골라 골드로 특화한다. 고르면 그 타워는 다른 물건이 된다 —
배율이 바뀌고, 상성 행이 통째로 다시 쓰이고, 고유 특성이 붙는다.

### 설계 규칙 셋

1. 모든 분기는 **주고받는다**. 얻는 것이 있으면 잃는 것이 있다.
2. 세 분기의 총 DPS는 비슷하게 두고 **어디에 쓰이는지**를 갈라 놓는다.
   그래야 "무조건 이거"가 없고 층 편성을 보고 고른다.
3. 상성은 **곱한다**. 원래 잘 잡던 것을 더 잘 잡거나, 원래 못 잡던 것을
   잡을 수 있게 되거나 — 둘 중 하나지 둘 다는 아니다.

### 분기 18종

| 타워 | 분기 | 배율 | 상성 | 고유 |
|---|---|---|---|---|
| 🏹 화살탑 | 💨 속사 | 공격 ×0.72 공속 ×1.70 | 소형 ×1.15 | — |
| | 🎯 관통 | 공격 ×1.30 사거리 ×1.10 | 중형 ×1.40 | 방어 관통 +3 |
| | 🏹 공성 | 공격 ×2.80 공속 ×0.42 | 소형 ×0.55 대형 ×2.10 | — |
| ❄️ 서리탑 | 🧊 혹한 | 공격 ×0.85 공속 ×1.10 | — | 감속 70% · 지속 ×1.7 |
| | 💎 서릿발 | 공격 ×1.65 공속 ×0.90 | — | 감속 30%로 약화 · **느려진 적에게 ×1.7** |
| | 🌨️ 눈보라 | 공격 ×0.95 사거리 ×1.20 | 비행 ×2.00 | **범위 감속**(46px) |
| 💣 대포탑 | 💥 융단 | 공격 ×0.72 공속 ×1.35 | 소형 ×1.60 | 범위 40 → 72 |
| | 🛡️ 철갑탄 | 공격 ×1.32 공속 ×0.85 | 대형 ×1.30 | 방어 무시 · 범위 40 → 22 |
| | 🚀 박격 | 공격 ×1.25 사거리 ×1.75 | 비행 ×3.83 | — |
| 🎯 저격탑 | 🎯 헤드샷 | 공격 ×1.20 | — | 32% 치명 ×2.6 |
| | 🔫 연사 저격 | 공격 ×0.50 공속 ×2.40 사거리 ×0.60 | — | — |
| | 💀 대물 저격 | 공격 ×2.10 공속 ×0.52 | 대형 ×1.35 | **HP 14% 이하 즉시 처형** |
| ⚡ 번개탑 | ⛓️ 연쇄 증폭 | 공격 ×0.78 | — | 연쇄 2 → 5 · 연쇄 사거리 ×1.35 |
| | 🔋 과충전 | 공격 ×1.75 공속 ×0.68 | — | 연쇄 없음 · 18% 감전(0.5초) |
| | 🛩️ 대공 관제 | 공격 ×1.15 사거리 ×1.45 | 비행 ×1.40 지상 ×0.6~0.75 | — |
| ☠️ 독탑 | ☠️ 맹독 | 공격 ×1.10 | — | 장판 피해 ×2.0 · 지속 ×0.65 |
| | 🌫️ 확산 | 공속 ×1.15 | — | 반경 ×1.55 · 지속 ×1.45 · 피해 ×0.65 |
| | 🧪 부식 | 공격 ×1.05 | 중형 ×1.15 | **장판 위 적의 방어력 소거 + 받는 피해 +30%** |

### 실측 — 세 갈래가 서로 다른 곳을 본다

등급별 실효 DPS(★5 기준). 총량은 비슷한데 향하는 곳이 갈린다.

| | 기본 | 분기 1 | 분기 2 | 분기 3 |
|---|---|---|---|---|
| 화살탑 S / M / L | 37 / 29 / 18 | 속사 **52** / 36 / 22 | 관통 46 / **52** / 22 | 공성 24 / 35 / **44** |
| 대포탑 S / L / A | 38 / 89 / 18 | 융단 **60** / 87 / 17 | 철갑탄 43 / **130** / 20 | 박격 31 / 73 / **56** |
| 번개탑 A | 196 | 연쇄 152 (6마리 타격) | 과충전 234 | 대공 **284** |
| 서리탑 A | 7 | 혹한 6 (감속 70%) | 서릿발 10 | 눈보라 **12** + 범위 감속 |

🧪 부식의 값어치는 자기 DPS가 아니다 — 방어력 30짜리 강철오크에게
화살탑(공격 18)이 평소 **1**을 넣는데, 부식 장판 위에서는 **14**를 넣는다.
혼자서는 약하고, 옆에 선 타워 전부를 세게 만든다.

### ⚠️ 분기를 섞지 않기로 한 이유

다른 분기끼리 합쳐 두 특성을 갖게 하는 안을 검토했고, **하지 않기로 했다**.

1. **분기가 전부 트레이드오프라서 겹치면 서로를 지운다.**
   속사(공속 ×1.70 · 공격 ×0.72)와 공성(공속 ×0.42 · 공격 ×2.80)을 합치면
   공속 ×0.71 · 공격 ×2.02 — 그냥 못한 공성이 된다. 값을 두 배 내고 나빠진다.
2. **잡을 수가 없다.** 6종 × 3분기 = 18개를 4등급에 맞춰 잡는 것도 벅찬데,
   교차 조합까지 열면 18쌍이 더 붙어 36개 × 4등급 = 144개를 잡아야 한다.
3. **선택이 사라진다.** 셋 중 하나를 고르는 것이 이 시스템의 전부인데
   결국 다 가질 수 있으면 그건 선택이 아니라 순서다.

대신 **재분기**를 뒀다 — 값을 더 내고 갈아탈 수는 있되, 두 개를 동시에 갖지는 못한다.
층 편성이 바뀌어서 판단이 달라졌을 때 되돌릴 길은 열어 두고,
"다 모으기"는 막는다.

| | 값 |
|---|---|
| 분기 개방 | 타워 ★5 도달 (레벨을 소모하지 않는다) |
| 분기 값 | 타워 기본가 × 12 (화살탑 60 · 대포탑 216 · 저격탑 312) |
| 재분기 값 | 타워 기본가 × 26 (화살탑 130 · 대포탑 468 · 저격탑 676) |

### 표시

- 격자의 타워에 **분기 아이콘 배지**가 붙는다 — 패널을 열어 봐야 아는 정보는 표시가 아니다.
- 타워 패널의 등급별 피해 줄(S/M/L/A)이 **분기를 반영해 다시 계산**된다.
- 준비 화면 말풍선과 마을 배치 현황에도 분기가 나온다.

### 곁다리로 고친 것

- **범위 감속이 감속을 안 걸었다** — `p.splash` 블록이 피해만 주고 slow를 옮기지 않아,
  🌨️ 눈보라의 "착탄 범위 감속"이 그냥 범위 피해였다. 감속을 든 발사체는 범위에도 남긴다.
- 한국어 줄바꿈 헬퍼 `wrapLines()`를 추가했다. 띄어쓰기가 드물어 어절 단위로만
  자르면 한 줄이 통째로 넘치므로, 칸보다 긴 어절은 글자 단위로 다시 자른다.

---

## v11.17 요약 — 영웅 액티브 스킬 (기획서 v4.17)

게임 버전 **v0.9.8**. 영웅 스킬은 지금까지 전부 패시브 스탯 블록이었다.
장착하면 숫자가 오르고, 그걸로 끝이었다 — 전투 중에 플레이어가 누를 것이 하나도 없었다.
상점의 **MP 포션**은 그래서 무용지물이었다. 쓸 MP가 없었으니까.

### ⚡ 액티브 8종

| 스킬 | 등급 | 값 | MP | 재사용 | 작용 | 효과 |
|---|---|---|---|---|---|---|
| ⚡ 과부하 명령 | epic | 120 | 30 | 24초 | 🏰 상단 | 모든 타워를 한꺼번에 과부하 |
| ☄️ 유성 낙하 | epic | 130 | 35 | 20초 | 🏰 상단 | 상단 적 전체에 영웅 공격력 ×2.2 |
| 🧱 성벽 결계 | rare | 88 | 25 | 26초 | 🏰 상단 | 6초간 기지 피해 전부 차단 |
| 🕐 시간 정지 | epic | 126 | 30 | 22초 | ↕️ 양쪽 | 상·하단 전체 3.5초 둔화 |
| 💥 심판 | rare | 80 | 20 | 12초 | ⚔️ 하단 | 가장 강한 몹에게 ×4.5 |
| 💚 재정비 | rare | 84 | 25 | 18초 | ⚔️ 하단 | 부대 전체 최대 HP의 30% 회복 |
| 🌪 소집 | common | 64 | 22 | 16초 | ⚔️ 하단 | 몹을 영웅 쪽으로 끌고 ×1.6 |
| 💰 약탈 명령 | common | 60 | 18 | 20초 | ↕️ 양쪽 | 층에 비례한 즉시 골드 |

**하단에 서 있으면서 상단을 바꾸는** 스킬이 절반이다(⚡☄️🧱🕐).
두 전선이 서로를 못 본 채 각자 돌아가던 것을, 영웅 한 명이 잇는다.

### 자동 / 수동

전투 하단 바에 **💧 MP 막대 · 🤖자동/👆수동 토글 · 칸 수만큼의 스킬 버튼**이 붙는다.
자동이면 `updateHeroActives`가 쿨과 MP가 차는 대로 알아서 쓴다.
수동이면 버튼을 눌러야 나가고, 못 쓰면 이유(`재사용까지 N초` / `💧MP N 필요`)를 띄운다.
버튼은 쿨다운이 차오르는 막대로 그려지고, 쓸 수 있게 되면 이름이 뜬다.
토글은 저장된다(`heroSkillAuto`).

### MP를 실제 자원으로

| | 값 |
|---|---|
| 기본 최대 MP | `40 + (레벨-1) × 6` |
| 기본 회복 | 3.2/s — **웨이브 중에만** |
| 웨이브 시작 | 최대치의 50%까지 채워 준다 |
| 💧 MP 포션 (10골드) | 즉시 만충 — 웨이브 밖에서는 이것뿐 |

웨이브 사이에는 회복이 없다. 그래서 포션이 살 이유가 생겼다.

**MP를 키우는 곳** — 🏪영웅 상점에 트랙 3개, 캠프 스킬트리에 1개를 새로 뒀다.

| 어디 | 이름 | 효과 |
|---|---|---|
| 🏪 상점 Lv.1 | 💧 마력의 그릇 | 최대 MP +5%/레벨 |
| 🏪 상점 Lv.3 | 🧘 명상 | MP 회복 +4.5%/레벨 |
| 🏪 상점 Lv.7 | ⏱️ 신속한 시전 | 액티브 재사용 −1%p/레벨 (최대 20, 하한 ×0.45) |
| 👑 스킬트리 | 💧 마력 순환 | 최대 MP +6% · 회복 +5% (레벨당) |

실측(상점 만렙): 최대 MP 40 → **264**, 회복 3.2 → **9.19/s**, 재사용 ×1.0 → **×0.8**.

### 칸

액티브 칸은 **영웅 Lv.1 / Lv.5**에 하나씩 열린다(2칸).
👑 영웅 상세 화면에 **⚡ 액티브 칸**과 **배운 액티브** 목록이 생겼다 —
목록을 탭하면 빈 칸에 끼우고, 같은 것을 다시 탭하면 뺀다.
상점에서 사면 빈 칸에 바로 들어간다.

### ⚡ 과부하 쿨다운이 보인다

예전에는 타워를 눌러 봐야 "재사용까지 N초"를 알았다 — 쓸 수 있는 줄 모르면 없는 기능이다.
상단 기지 HP 막대 오른쪽에 **74×15 게이지**를 붙였다.
준비되면 `⚡ 과부하 준비`(금색 테두리), 쿨 중이면 `⚡ Ns`와 차오르는 막대.
🧱 성벽 결계가 걸려 있으면 기지 둘레에 고리가 돈다.

### 곁다리로 고친 것

- **기지 HP 표시가 `200.36476000000158/100`이었다** — 소수점 그대로에, 최대치는 강화를 반영하지 않는 상수 `BASE_HP_MAX`를 썼다. `Math.ceil` + `baseHpMax()`로 고쳤다.
- **저장에서 액티브가 사라졌다** — `normalizeHeroGear`가 `actives`/`activeSlots`를 복원하지 않아, 새로고침하면 배운 액티브가 전부 날아갔다.

---

## v11.16 요약 — 강화 하나가 30레벨까지 (기획서 v4.16)

게임 버전 **v0.9.7**. v11.15에서 "30개"를 **트랙 수**로 잘못 읽었다.
요청은 **강화 하나가 30레벨까지 오른다**는 뜻이었다.

### 곡선을 통째로 다시 잡았다

10번 사는 곡선을 그대로 30번 쓰면 효과는 6.2배, 비용은 4만 6천 배가 된다.
둘 다 다시 풀었다.

| | 이전(10레벨) | 이후(30레벨) |
|---|---|---|
| 기본 상한 | 10 (짧은 트랙 8) | **30** (짧은 트랙 24) |
| `step` | — | **×0.6** |
| `growth` | — | **÷3** |
| `costMult` | 1.42~1.58 | **1.16~1.22** (`1+(m-1)/2.6`) |
| 트랙 하나 총효과 | 39.2% | **139.3%** (약 1.8배) |
| 트랙 하나 총비용 | 약 800 | **9,934** |
| 마을 총 사용처 | 약 39만 | **약 162만** |

효과가 1.8배로 늘고 비용이 12배로 늘었다 — 같은 힘을 사려면 훨씬 오래 벌어야 한다.
`step × 0.6`, `growth ÷ 3`은 "30번 산 총효과 ≈ 옛 10번의 1.8배"를 풀어서 나온 값이다.

### 트랙 상한: `3 + 레벨 × 3`

Lv.1이면 3번, 레벨마다 +3, **Lv.10에서 30번**. 30레벨을 점으로 찍으면 읽히지 않으므로
목록 표시를 **막대**로 바꿨다 — 채운 칸(초록) / 지금 살 수 있는 칸(회색) / 승급해야 열리는 칸(어둡게).

### 승급 조건을 고쳤다 — 도달 불가능한 조건이었다

v11.15에서 `다음 레벨 × 3`회로 뒀는데, 30레벨 개편과 함께 8회로 올리면서 **버그가 났다**.
Lv.1에서 열려 있는 트랙은 건물당 1~2개뿐이고 트랙당 3번까지만 살 수 있으니
살 수 있는 최대가 3~6번인데 8번을 요구했다 — **대장간은 영영 Lv.2가 될 수 없었다.**

지금 살 수 있는 양에 **비례**시켰다: `ceil(이 레벨에서 살 수 있는 총 횟수 × 0.5)`.
구조상 항상 닿을 수 있고 뜻도 그대로다 — "이 레벨에서 할 수 있는 걸 절반은 하고 올라가라."

전 건물 × 전 레벨에서 도달 가능함을 확인했다.

| | Lv.1 살 수 있는 최대 | 필요 | 전 레벨 |
|---|---|---|---|
| 공방·병영·상점·여관·성채 | 6 | 3 | OK |
| 대장간 | 3 | 2 | OK |

### 골드 예산별 도달점 (탐욕적으로 싼 것부터 구매)

| 골드 | 건물 Lv | 타워 공격 배율 | 아군 공격력 | 성채 공격력 | 남은 골드 |
|---|---|---|---|---|---|
| 5,000 | 2 | ×1.23 | 30 | — | 0 |
| 20,000 | 3~4 | ×1.35 | 49 | 59 | 224 |
| 60,000 | 4~5 | ×2.47 | 66 | 98 | 34 |
| 150,000 | 5~6 | ×3.16 | 85 | 132 | 10 |
| 400,000 | 7~8 | ×3.98 | 127 | 210 | 285 |

**모든 구간에서 골드가 남지 않는다.** 예전에 12만이 남던 자리가 이제 Lv.5~6이고,
그마저도 한 판에 다 못 쓴다. 상한값 표시도 실제 상한에 맞췄다 —
가시 방벽 89%라 적어놓고 75%만 걸리면 거짓말이다.

---

## v11.15 요약 — 🏰성채와 마을 경제 재설계 (기획서 v4.15)

게임 버전 **v0.9.6**. 마을이 골드를 다 못 먹고 있었다.

### 진단 — 실측 12만 골드가 남았다

15층 자동 주행에서 골드가 **129,190** 남았다. 살 것이 바닥나면 후반의 모든 판단이
"어차피 남으니까 아무거나"가 된다. 세 갈래로 조였다.

| | 이전 | 이후 |
|---|---|---|
| 건설비 (합) | 185 | **351** (약 1.9배) |
| 레벨업비 배수 | 1.70~1.72 | **1.85** (성채 1.92) |
| 트랙 기본 비용 | — | **1.5배** |
| 트랙 수 | 31 | **52** |
| 마을 총 사용처 | 약 12만 | **약 39만** |

### ① 🏰 성채 — 마을의 축

늘 지어져 있는 새 건물이다. **성채 레벨이 다른 모든 건물의 레벨 상한**을 정한다
(`castleLevel + 1`). "무엇을 먼저 올릴까"가 아니라 **"성채를 언제 올릴까"**가 첫 판단이 된다.

> 제안은 "성채를 올리면 다른 건물 레벨도 같이 오른다"였는데, 그러면 건물별 레벨업 비용이
> 의미를 잃는다(같은 요청의 ②와 충돌). **상한**으로 바꾸면 성채가 필수가 되면서도
> 어느 건물을 먼저 올릴지는 여전히 선택으로 남는다.

강화 10종 — 다른 건물과 겹치지 않는다. 여기만 **기지 자체**를 만진다.

| 강화 | 여는 레벨 | 효과 |
|---|---|---|
| 🧱 성벽 증축 | 1 | 기지 최대 HP |
| 🛡️ 강철 성체 | 1 | 기지 피해 감소 |
| ❤️ 자가 회복 | 2 | 웨이브 중 기지 재생 |
| 🏹 **최후 저지선** | 3 | **성채가 직접 공격** (사거리 1.6칸 · 0.7발/s) |
| 🌵 **가시 방벽** | 4 | 돌진해 오는 적을 확률로 튕겨낸다 |
| 🚪 철문 보강 | 5 | 기지에 닿은 적의 피해 감소 |
| 🔭 망루 증축 | 6 | 성채 사거리·공격속도 |
| ⚙️ 권양기 | 7 | 과부하 쿨다운 |
| 📦 비축 창고 | 8 | 매 층 시작 골드 |
| ♾️ 불멸의 성 | 10 | 최대 HP % · 피해 감소 (무한) |

실측(성채 Lv.10 · 전부 최대): 공격력 97 · 사거리 146px · 차단 48% ·
기지 HP 345 · 피해 감소 27% · 관문 피해 감소 25% · 재생 3.85/s.
18초 웨이브에서 성채가 7발을 쏴 679 피해. 차단 48%는 돌진 피해를 **49% 줄인다**.

### ② 승급에 조건을 건다

골드만 있으면 다 되는 구조에서는 "무엇을 먼저 올릴까"가 선택이 아니다. 두 겹으로 건다.

- **전역** — 🏰성채 레벨 + 1 이 상한
- **지역** — 그 건물에서 산 강화가 `다음 레벨 × 3`회 이상

버튼에 무엇이 모자란지 그대로 적는다 — `🏰 성채 Lv.3 필요` / `강화 4/6 더 필요`.
눌러 보고 알게 하면 안 된다.

### ③ 한 번에 다 못 찍게 한다

트랙 하나를 살 수 있는 횟수를 **건물 레벨이 정한다**: `2 + 레벨 × 1`.
Lv.1이면 2번, Lv.10이 돼야 10번까지 간다. 목록의 점은 트랙의 진짜 상한만큼 찍되
지금 못 사는 칸은 흐리게 두고, 레벨에 막힌 것은 `MAX`가 아니라 **`승급 필요`**로 적는다 —
끝난 것과 막힌 것은 다른 상태이고, 다르게 보여야 승급이 목표가 된다.

### ④ 트랙 31 → 52

건물마다 7~10개. 새로 넣은 것: 예광탄·철갑 촉·냉각 코팅(공방), 야전 의무·연계 훈련(병영),
성수 배급·각인 연구(상점), 보급 계약·단골 손님(여관), 감정사·선별 수거(대장간), 성채 10종.

> "30개 정도"는 **마을 전체 기준**으로 읽었다. 건물마다 30개면 150개가 되어
> 목록이 스크롤로도 감당이 안 된다.

### 검토 — 심연 → 트리 → 재도전 루프

마을을 그 레벨까지 키우는 데 드는 누적 골드:

| 건물 Lv | 1 | 3 | 5 | 7 | 8 | 10 |
|---|---|---|---|---|---|---|
| 누적 골드 | 994 | 8,063 | 33,995 | 131,523 | 172,705 | 296,164 |

예전에 남아돌던 12만이 이제 **딱 Lv.7 언저리**다. 즉 한 판에 마을을 다 못 키운다.
골드는 판마다 리셋되므로 "이번 판에 어디까지 키울까"가 매번 새로 결정되고,
그 상한을 밀어 올리는 것은 보석(스킬 트리·대장간·승천)뿐이다 —
**심연에서 골드로 마을을 키워 깊이를 벌고, 그 깊이가 보석이 되어 영구 상한을 올리고,
다시 내려간다**는 고리가 이제 실제로 닫힌다.

---

## v11.14 요약 — 훈련 건너뛰기와 스킬 초기화 (기획서 v4.14)

게임 버전 **v0.9.5**.

### ① ⏭ 훈련 건너뛰기 — 💎+3, 바로 심연으로

**보고: "훈련모드 건너뛰기를 만들어달라 했던 것 같은데 없어."** — 맞다.
v11.11에서 만든 것은 **안내(튜토리얼) 쪽지** 건너뛰기였고, 요청은 **훈련 모드**였다.
같은 이름의 다른 것을 고쳤다.

훈련은 6웨이브짜리 연습판이고 심연을 여는 조건이다. 완주하면 2보석,
첫 클리어 정산까지 합쳐 최대 4보석을 번다. 초기화를 반복하면 그 6웨이브를
매번 다시 치러야 심연에 들어갈 수 있다 — 아는 내용을 다시 치르게 하는 것은 값이 아니다.

캠프 › 출격 탭의 "∞ 심연 — 아직 잠겨 있습니다" 칸에 **⏭ 훈련 건너뛰기 💎+3**을 붙였다.
누르면 평균치에 가까운 3보석을 받고 심연이 바로 열린다.

`trainSkipped`는 세이브에 남지만 **데이터 초기화에서는 함께 지워진다** —
훈련은 '설명'이 아니라 '진행'이므로 초기화의 뜻을 흐리면 안 된다.
대신 초기화 직후 캠프에 버튼이 그대로 있으니 한 번만 누르면 된다.
(안내 쪽지 표시 `df_tut_seen`은 반대로 초기화에서도 살아남는다 — 그쪽은 설명이니까.)

### ② ♻️ 스킬 초기화 — 무료 · 전액 반환

트리 전체가 **4126보석**짜리다. 그 규모에서 한 번 잘못 찍은 것이 판을 통째로 묶어 두면
플레이어가 하는 일은 '고민'이 아니라 '검색'이 된다. 되돌릴 수 있어야 실험이 선택이 된다.

🌳 스킬 탭 위에 **♻️ 스킬 초기화 — 무료 · 💎N 전액 반환** 줄을 뒀다.
넣은 보석을 한 푼도 빼지 않고 돌려주고 트리를 비운다. 값은 받지 않는다.
되돌릴 수 없는 조작이라 **두 번 눌러야** 실행된다 (타이틀 리셋과 같은 규칙, 5초 안에).

실측: 30보석어치(2노드 ×5레벨)를 찍은 상태에서 초기화 → 레벨 0, 보석 30 전액 복귀,
`towerDmgMult`가 1.0으로 재계산.

---

## v11.13 요약 — 머지는 됐는데 화면이 안 바뀌던 이유 (기획서 v4.13)

게임 버전 **v0.9.4**. 코드 변경은 없고, **배포가 도착하지 않던 것**을 고쳤다.

**보고: "0.9 버전에서 버전이 더 안 바뀌고 있는데 메인으로 머지됐는지 확인해봐."**

머지는 정상이었다 — `origin/main`은 v0.9.3이고 브랜치와 차이가 없었다.
문제는 **서비스 워커**였다.

```js
const CACHE = 'dual-frontier-v0.8.0';   // ← PWA를 붙인 v0.8.0 이후 한 번도 안 바뀜
```

`activate`는 **"키가 다른 캐시만"** 지운다. 키가 그대로면 아무것도 지워지지 않는다.
게다가 `fetch`는 전부 캐시 우선이라 네트워크를 아예 보지 않았다.
즉 **한 번 설치한 사람은 무엇을 머지하든 영영 v0.8.0 파일을 받고 있었다.**
게임 버전은 v0.9.0 → v0.9.3까지 네 번 올렸는데 캐시 키는 네 번 다 잊었다.

두 군데에 적는 값은 반드시 어긋난다. 그래서 두 가지를 고쳤다.

### ① 버전을 한 군데로 합쳤다

`js/version.js` 하나가 유일한 정의처다. 게임(`constants.js`)과 서비스 워커(`sw.js`)가
같은 파일을 읽는다 — SW는 `importScripts('js/version.js')`로 가져와
`CACHE = 'dual-frontier-' + self.GAME_VERSION`을 만든다. 이제 어긋날 수가 없다.

### ② 코드는 네트워크 우선, 그림·소리는 캐시 우선

키를 합쳐도 "올리는 것을 잊는" 실수는 다시 날 수 있다.
`.js` · `.html` · 내비게이션은 **네트워크를 먼저 보고**, 실패하면 캐시로 떨어진다 —
키를 잊어도 다음 접속에서 저절로 최신이 된다. 받아온 것은 캐시에 덮어쓰므로
오프라인 동작은 그대로다. 그림·소리는 용량이 크고 거의 안 바뀌므로 예전처럼 캐시 우선.

검증: `dual-frontier-v0.8.0` 캐시에 낡은 파일을 심어 둔 클라이언트가
새 SW를 받자 옛 캐시가 삭제되고 v0.9.4가 서빙되는 것을 확인했다.

---

## v11.12 요약 — 감액 옆에 남아 있던 정액 (기획서 v4.12)

게임 버전 **v0.9.3**. 보고 세 건.

### ① 준비 화면에서 ⏸·배속·🔊가 정보를 가렸다

버튼 세 개는 폭 44 + 간격 5로 **142px**인데, 브리핑은 오른쪽 **118px**만 비워 두고 있었다.
30px이 겹쳐서 "최고 13층"이 "최"로, "💎 되짚는 층"이 "💎 되짚는"으로 잘렸다.

여백을 늘리는 방식으로는 글자 길이가 바뀔 때마다 다시 어긋난다.
아예 **전용 띠(38px)**를 잡고 본문을 그만큼 내렸다 — 겹칠 자리가 구조적으로 없어진다.
띠 왼쪽이 비니까 "∞ N층 / 되짚는 층 ×0.1"을 그리로 옮겼다. 스크롤해도 늘 보이는 자리다.
본문에서 중복되던 층 제목은 뺐고, 그만큼 브리핑이 한 화면에 들어온다.

### ② 되짚기 감액 옆에 정액 보석이 남아 있었다

**보고: "여전히 심연에서 한 스테이지만 클리어하고 중지하면 보석을 하나 준다.
(기존에 클리어했던 스테이지를 다시 하는 것)"**

정산식은 이렇게 생겼다:

```
endTerm(층 적립) + caveTerm(케이브 레벨) + killTerm(처치÷60) + recTerm(새 깊이)
```

되짚는 층은 `ENDLESS_REPEAT_MULT = 0.1`이 걸려 `endTerm`이 0.024 → 내림 0이고,
새 깊이가 없으니 `recTerm`도 0이다. 그런데 **`caveTerm`은 새 판에서도 최소 1**이다.
0 + **1** + 0 + 0 = 1 — 이것이 그 보석 하나였다.

층 적립에만 감액을 걸고 옆에 깊이와 무관한 정액 항을 두면 그 감액은 통째로 무의미해진다.
케이브 레벨과 처치 수도 **이번 판에서 새로 판 깊이의 비중**만큼만 받게 했다.

```
repeatSideMult(cleared, best) = 0.1 + 0.9 × (새로 판 층 / 돌파한 층)
```

전부 되짚기면 ×0.1, 전부 새 깊이면 ×1.0, 반반이면 ×0.55.

| 상황 | 이전 | 이후 |
|---|---|---|
| 기록 13 · 1층 깨고 나감 | **1** | **0** |
| 기록 13 · 5층 깨고 나감 | 4 | 1 |
| 기록 13 · 10층 되짚기 | 6 | 1 |
| 기록 0 · 1층 (첫 판) | 2 | 2 |
| 기록 13 · 14층 (신기록) | 7 | 7 |

결과 화면의 내역도 같은 값을 보여준다 — `케이브 레벨 0.1 · 되짚기 ×0.10`.

### ③ 자연 회복 0 → 0.5%/s

v11.11에서 아예 0으로 뒀는데, 회복이 웨이브 **사이**뿐 아니라 웨이브 **안**에서도 사라져
초반 아레나가 지나치게 팍팍해졌다. 0.5%/s로 되돌린다 —
60초를 온전히 쉬어야 30%다. 있는 듯 없는 듯한 몫만 남기고,
실질적인 재생은 여전히 카드·장비·스킬로 사게 한다.

---

## v11.11 요약 — 회복을 사게 만들고, 축을 하나씩 늘린다 (기획서 v4.11)

게임 버전 **v0.9.2**. 플레이 보고 여섯 건.

### ① ⏭ 안내 건너뛰기 — 💎+3, 그리고 다시는 안 뜬다

안내를 끝까지 보면 대략 4보석을 벌었다. 건너뛰면 그 평균치에 가까운 **3보석**을 대신 준다 —
"빨리 시작하는 값으로 보석을 포기하라"는 교환이 되면 테스트든 새 출발이든 매번 성가시다.

한 번 끝냈거나 건너뛴 표시(`df_tut_seen`)는 **데이터 초기화에서도 살아남는다.**
초기화의 뜻은 "진행을 처음부터"이지 "설명을 다시 듣기"가 아니다.
캠프 › 📖 안내 다시 보기로는 여전히 볼 수 있다.

### ② 자연 회복을 없앴다 — 🏨여관이 할 일이 생기도록

전투가 끝났을 때 부대는 늘 **"죽었거나 멀쩡하거나"** 둘 중 하나였다. 그 사이가 없으면
여관의 회복은 살 이유가 없는 상품이다. 원인은 전투 이탈 자연 회복 **4.5%/s** —
4초만 안 맞으면 22초 만에 만피가 됐다.

| 항목 | 이전 | 이후 |
|---|---|---|
| 전투 이탈 자연 회복 | 4.5%/s | **0** (기본값) |
| 웨이브 후 무상 회복 | 30% | **16%** |
| 처치 시 회복 (카드) | +5 | **+2** |
| 처치 시 회복 (트리) | +2/Lv | **+0.8/Lv** |
| 영웅 재생 (카드) | 3/s | **1.2/s** |
| 영웅 재생 (트리) | 1.5/Lv | **0.5/Lv** |
| ❤️ 응급 치료 드랍 | 15% | **10%** |
| 장비 재생 옵션 | — | 전부 절반 |

재생은 이제 **사는 것**이다. `regenBonus`는 "기본값에 곱하는 보너스"가 아니라
초당 최대 HP 비율 그 자체로 의미가 바뀌었고, 새 카드 두 장(🩹응급 처치 0.8%/s,
⛑️야전 의무대 2%/s)이 그것을 판다.

### ③ 🗡️ 도적 — 은신 · 기습 · 탐욕

다른 용병은 전부 "얼마나 세게 맞고 얼마나 세게 때리나"뿐이라 조합이 앞줄/뒷줄 두 갈래로
수렴했다. 도적은 그 축 밖에 선다.

- **은신** (7초 쿨다운, 2.6초) — 사라진다. 그동안 **표적이 되지 않고 맞지도 않는다.**
  이동속도는 1.45배 — 파고들라고 준다.
- **기습** — 은신을 풀며 넣는 첫 타격이 **3.2배**.
- **탐욕** (55%) — 바닥에 드랍이 떨어지면 확률적으로 주우러 간다.
  알아서 챙기는 대신 그동안 전열에서 빠지므로, 쓸모와 손해가 같이 온다.

여관에만 있던 기존 도적은 **일반 용병(💎10 해금)**으로 내보내고,
그 자리는 🪓광전사가 메운다 — 여관의 첫 손님은 "싸고 빠른 근접"이어야 조합이 이어진다.

### ④ ☠️ 독탑 — 자리에 값을 남기는 유일한 탑

다른 탑은 전부 "지나가는 순간에만" 값을 낸다. 독탑은 한 발이 느리고(0.45/s) 약한 대신
착탄점에 **반경 34px · 4초짜리 장판**을 깐다. 장판은 초당 그 탑 실효 공격력의 55%를 준다.

밀집한 무리와 느린 대형에게 강하고(large ×1.55), 비행은 그냥 지나간다(air ×0.35).
겹쳐 깔면 중첩되지만 아주 가까운 자리는 시간만 새로 채워서 도배가 무한 중첩이 되지 않게 했다.
💎13에 해금.

### ⑤ 👑 상단 영웅 — 한 번 지나갈 때 한 대

예전에는 **"1초마다, 반경 안 모든 적의 공격력 합"**을 통째로 맞았다. 경로 한가운데 서면
지나가는 무리 전체가 매 초 다시 계산되므로 30마리 웨이브에서 영웅이 몇 초 만에 녹았다 —
상단에 세울 이유가 사라진다.

- 적 하나는 **한 번 지나갈 때 한 대만** 친다. 나갔다 다시 들어오면 그때 또 한 대다.
- 대신 겹친 적은 **이동속도 ×0.45**로 느려진다. 상단에 세우는 것이 "몸으로 막는" 선택이 된다.
- **준비 단계에서도** 빈 칸을 탭해 영웅 자리를 잡을 수 있다.
  웨이브가 시작된 뒤에야 옮길 수 있으면 첫 몇 초는 늘 엉뚱한 곳에 서 있었다.

실측: 적 13마리가 지나가는 13초 동안 영웅 HP 80 → 77.

### ⑥ 심연 1층에서 타워 하나가 한 마리도 못 잡았다

v0.9.0에서 강화를 전부 배율로 바꿨는데 **화살탑 기본 공격력이 2**였다.
6% 강화는 2.12 — 반올림하면 그대로 2다. 즉 1층에서 골드로 살 수 있는 것이 아무 효과가 없었고,
한 대 2로는 체력 14짜리 고블린을 지나가는 동안 죽이지 못해 계속 표적만 바꿨다.

**배율이 물릴 바닥이 있어야 배율형 강화가 작동한다.** 모든 탑의 기본 공격력을 1.7배로 올렸다
(화살 2→3.4, 서리 1→1.7, 대포 8→13.6, 저격 18→30, 번개 6→10).
깊은 층이 같이 쉬워지지 않도록 `ENDLESS_EXP`를 1.062→1.068로 올려 상쇄했다.

| 플레이어 | 1층 | 8층 | 12층 | 16층 | 20층 | 26층 | 40층 |
|---|---|---|---|---|---|---|---|
| 신규 (0%)   | 27 | 3.4 | 0.88 | 0.16 | — | — | — |
| 중반 (60%)  | 733 | 102 | 31 | 7.3 | 1.2 | 0.59 | 0.09 |
| 만렙 (100%) | 4448 | 620 | 190 | 44 | 7.2 | 3.8 | 0.63 |

1층 한 대 3(고블린 상성 ×1.25 = 4)으로 체력 14짜리를 네 대에 잡는다.
한 타워 예산 180 vs 웨이브 총 체력 154 — 사격 가동률을 감안하면 **절반 남짓**을 잡는다.

---

## v11.10 요약 — 기다리는 시간과 다시 하는 수고 (기획서 v4.10)

게임 버전 **v0.9.1**. 셋 다 "이미 이긴 판을 다시 치르게 만드는 것"을 없앤다.

### ① 배속 5단계 — 최대 10배속

캠프 강화가 쌓이면 초반 층은 타워 두어 개로도 밀린다. 그때부터 60초 웨이브는
난이도가 아니라 **대기 시간**이다. `SPEED_STEPS`를 `[1,2,3]` → **`[1,2,3,5,10]`**으로 늘렸다.

스테이지 스킵은 넣지 않았다 — 층을 지나며 받는 골드와 강화 카드가 진행의 뼈대라
건너뛰면 그 층의 성장이 통째로 사라진다. 대신 같은 판을 열 배 빨리 치르게 한다.

루프가 `dt`를 키우는 게 아니라 `update(dt)`를 N번 도는 방식이라
10배속에서도 충돌 판정과 투사체가 건너뛰지 않는다. 배속이 정수여야 하는 이유이기도 하다.
수동 조작 중에는 여전히 2배속으로 강등된다(`formation.js`) — 손으로 부대를 몰면서
10배속을 볼 수는 없다.

### ② 🔀 타워 이설 — 골드로 자리를 바꾼다

10층마다 경로가 바뀐다. 그때 경로 위에 놓인 타워는 자동으로 옆으로 밀려나지만
(`relocateTowersOffPath`), **잘 잡아둔 배치 전체**는 어긋난 채 남는다.
지금까지 유일한 수습은 팔고(회수 60%) 다시 짓는 것이었는데,
같은 종류를 지을수록 건설비가 오르는 규칙(`TOWER_COST_ESCALATION`)까지 겹쳐
사실상 "처음부터 다시 하라"는 말이었다.

| 동작 | 값 |
|---|---|
| 빈 칸으로 이동 | 투자액의 **15%** (최소 4💰) |
| 타워끼리 교환 | 두 타워 이설비의 **합** — 같은 값이면 정확히 두 배 |

🏰마을 › 타워배치에서 타워를 고르면 **🔀 이동** 버튼이 붙는다. 누르면 격자가
갈 수 있는 칸(파랑)과 맞바꿀 타워(보라)로 칠해지고, 한 칸을 고르면 끝난다.
레벨·투자액·처치 수는 그대로 따라간다 — 옮기는 것이지 다시 짓는 게 아니다.
경로 칸으로는 옮길 수 없고, 골드가 모자라면 거절한다(차감 없음).

### ③ 👑 영웅 배치를 기억한다

층이 넘어갈 때마다 `gs.hero.placement`가 `'none'`으로 풀렸다. 원래는
"층 구성에 맞춰 다시 고르라"는 뜻이었는데, 실제로는 **거의 항상 같은 자리를 다시 고르는
반복 조작**이 됐다 — 배치를 잊으면 출격 자체가 막히므로 잊을 수도 없었다.

마지막으로 고른 자리를 `heroPlacePref`로 영구 저장하고,
층이 넘어갈 때 · 전사 후 복귀할 때 · 새 판을 시작할 때 자동으로 되돌린다.
자리를 바꾸고 싶으면 예전처럼 버튼을 누르면 되고, 그 선택이 다시 기억된다.

---

## v11.9 요약 — 정액을 배율로, 그리고 보석이 갈 곳 (기획서 v4.9)

게임 버전 **v0.9.0**. 26층까지 플레이한 보고 열 건.

### ① 상단으로 밀고 들어오던 것을 되돌리고, 대신 보여준다

v11.8에서 후퇴·전멸 시 남은 몹의 몫을 상단 웨이브에 얹었다(spill). 그런데
**상단 타워만 키우면 하단을 통째로 버려도 되는 게임**이 됐다 — 위아래를 나눠 푸는 게
이 게임의 뼈대인데 그 뼈대를 흔들었다. 되돌렸다.

대신 **돌진 연출**을 넣었다. 후퇴하거나 전멸하면 남은 몹들이 실제로 화면 위쪽 성으로
달려가 부딪히고, 부딪힌 만큼 HP가 깎인다. 숫자로만 `-14HP`라고 뜨던 것을
"몇 마리를 못 막았는지"로 눈에 보이게 했다. 총 피해량은 같다.

### ② 상단이 26층까지 무피해로 막히던 이유 — 정액 공격력

화살탑 기본 공격력은 **2**다. 여기에 공방의 세 트랙(날붙이 연마·강화 탄두·정밀 세공)이
**+100을 통째로** 얹고 스킬 트리가 다시 +880을 얹었다. 그러면 타워 공격력의 99%가
"어느 탑이냐"와도 "몇 레벨이냐"와도 무관한 값이 되고, **적 체력 곡선이 아무 의미가 없어진다.**
10층에서 산 강화가 26층까지 그대로 통한 것이 정확히 이것이다.

정액을 전부 **배율**로 바꿨다. 이제 공격력은 `기본 × 레벨 × 강화배율`이라
탑의 종류와 레벨이 다시 의미를 갖고, 적 체력과 같은 축에서 겨룬다.

실측 여유배(60초 웨이브 총 화력 ÷ 웨이브 총 체력):

| 플레이어 | 1층 | 8층 | 12층 | 16층 | 20층 | 26층 | 32층 | 40층 |
|---|---|---|---|---|---|---|---|---|
| 신규 (0%)   | 15.5 | 1.8 | 0.41 | 0.07 | — | — | — | — |
| 초반 (25%)  | 58 | 8.4 | 2.5 | 0.50 | 0.06 | — | — | — |
| 중반 (60%)  | 428 | 62 | 19.5 | 4.6 | 0.74 | 0.39 | 0.39 | 0.05 |
| 만렙 (100%) | 2621 | 380 | 119 | 28 | 4.7 | 2.6 | 2.6 | 0.45 |

벽이 신규 10~12층 → 25% 14~16층 → 60% 20~22층 → 만렙 35~40층으로 옮겨간다.
곡선 자체도 함께 조였다(`ENDLESS_EXP` 1.055→1.062, `ENDLESS_LINEAR` 0.09→0.11).

### ③ 하단은 반대로 벽이었다 — 곱셈 방어력 vs 덧셈 공격력

하단 몹의 방어력은 체력과 **같은 곱셈 곡선**(`sm`)을 탔다. 그런데 아군 공격력은
마을 트랙과 스킬 트리가 정액으로 더해주는 값이다. 40층 마왕 방어력 **111**,
궁수 공격력 **117** — 유효타 6, 처치에 84초. "그냥 몹을 잡을 수 없는 난이도"라는
보고가 이것이다.

- 방어력은 `sm^0.45`, 공격력은 `sm^0.80`으로 지수를 눌렀다 (체력은 그대로).
- 방어력이 아무리 높아도 **원 공격력의 12%는 들어가는 하한**을 뒀다.
- 하단 곡선을 상단과 분리했다 (`ENDLESS_ARENA_LINEAR` 0.06, `ENDLESS_ARENA_EXP` 1.034).

같은 40층 마왕이 12.5초 → **2.9초**에 죽는다. 10층 1.5초에서 50층 24.5초까지
벽 없이 완만하게 오른다.

### ④ 스킬 트리 v2 — 나무 다섯 × 노드 아홉 × 레벨 열

| 항목 | v1 | v2 |
|---|---|---|
| 나무 | 3 (타워·영웅·보조) | **5** (타워·병력·영웅·기지·심연) |
| 노드 | 27, 한 번씩만 | **45, 각 10레벨** |
| 해금 | 바로 위 노드 1개 | **윗줄에 쌓은 레벨 ≥ 줄번호×5** |
| 비용 | 고정 (총 48💎) | **기본값 × 레벨** (총 **4126**💎) |

구 세이브의 습득 노드는 하나당 2보석으로 환급한다.
정액 공격력 노드(`강화 촉`)는 **얼음 도금**(모든 타워에 감속)으로 바뀌었다.

### ⑤ ⚒️ 대장간 — 값이 흔들리는 유일한 건물

다른 건물은 "골드를 내면 정해진 만큼 오른다". 대장간은 보석을 받고, 셋 중 둘은 확률이다.

- **🔨 장비 연마** — 장비가 아니라 **칸**에 +N을 붙인다. 장비는 한 판짜리지만 연마는
  남으므로 보석을 쓸 값어치가 있다. 칸마다 +10, 단계당 그 칸 장비 능력 +8%.
- **🔥 타워 합성** — ★5 심 둘 → ★6 (62%), ★9 둘 → ★10 (18%). 실패하면 하나가 탄다.
  가진 최고 별이 그 판의 **타워 최고 레벨**이 된다. ★10은 1레벨의 27배 화력.
- **🎲 담금질** — 숙련도를 걸고 굴린다. 실패하면 5단위 체크포인트로 되돌아간다.
  숙련도 1당 타워·아군 공격력 +2%, 최대 40 (+80%).

곁가지로 골드 트랙 5개(풀무 개량·고철 회수·성벽 담금질·장인의 눈·♾️끝없는 망치).

### ⑥ 🎴 부적 — 일회용 보석 사용처

캠프 출격 탭에서 💎8에 하나 뽑고, 두 칸에 끼우고, 출격하면 소모된다.
13종 · 등급별 가중치. "이번 판을 어떻게 풀 것인가"를 고르는 물건이다.

### ⑦ ♾️ 승천 — 끝이 없는 사용처

끝이 있는 사용처는 결국 다 채워진다. 그런데 층당 보석은 계속 들어온다.
해금 탭 아래에 값이 단계마다 12%씩 오르는 무한 트랙을 뒀다 — 아무리 벌어도 앞서가지 못한다.

**보석 사용처 총계**: 해금 63 + 스킬 4126 + 각인 + 대장간(연마 ~1200 · 합성 ~3000 · 담금질 ~1500)
+ 부적(무한) + 승천(무한). "140보석이 남는다"던 문제는 이걸로 닫힌다.

### ⑧ 용병 슬롯이 화면 가로를 넘었다

강화로 슬롯을 최대치까지 올리면 최소 폭 44px가 곱해져 오른쪽으로 넘어갔다.
줄당 개수를 화면 폭에서 계산해 **두 줄로 접는다**. 특수 용병 칸도 같이.

### ⑨ 강화 카드 정리 — 안 고르는 카드를 빼고 새로 넣었다

'즉시 골드 +15/+35'는 뺐다. 2층만 가도 한 판에 수백 골드가 도는데 강화 한 장을
그걸로 채우면 그 선택지는 없는 것과 같다. 정액 수치는 전부 배율로 바꿨다.

새 카드 10장: 관통 탄심 · 서리 코팅 · 과부하 개조 · 규격 부품 · 날렵한 손놀림 ·
급소 찌르기 · 정예 사냥터 · 보석 광맥 · 각인 공명 · 자동 수복.
**대가가 있는 카드**도 넣었다 — 집중 포화(공격력 +80% / 사거리 −20%),
결사대(아군 공격력 +70% / HP −25%).

---

## v11.8 요약 — 하단을 비우면 위로 올라온다 (기획서 v4.8)

게임 버전 **v0.8.0**. 플레이 보고 일곱 건.

### ① 강화 목록을 누르면 뒤의 포션이 팔렸다 (버그)

영웅 상점의 ⚒️강화 탭은 일반 건물 화면이 그린다. 그런데 **🛒매대를 그릴 때 기록해둔
버튼 좌표(`gs.ui.shopItemBtns`)가 지워지지 않은 채 남아** 있어서, 강화 버튼을 누르면
그 자리에 있던 포션이 먼저 팔렸다.

매대를 안 그리는 프레임에는 좌표를 지우고, 탭 처리도 `shopTab === 'buy'`일 때만
매대 버튼을 보게 이중으로 막았다.

### ② 화면을 쓸어넘기면 그게 탭이 됐다 (버그)

`beginDrag`는 **스크롤이 걸린 영역에서만** 손가락 이동을 쟀다. 전장 위에는 스크롤이 없으니
`_didDrag`가 서지 않았고, 손을 떼는 순간 그 자리가 탭으로 처리됐다 —
화면을 위아래로 훑을 때마다 영웅이 그리로 걸어가거나 타워가 과부하에 들어갔다.

누른 자리를 항상 기억하고(`_pressAt`), 어디서 시작했든 6px 이상 움직이면 탭이 아닌 것으로 친다.

> **타워가 멈추는 현상은 재현하지 못했다.** 탭 전환(12초 숨김) · 새로고침 ·
> 모바일 뷰포트 리사이즈 세 경로를 모두 시험했지만 발사는 계속됐다.
> 대신 그 증상이 나올 수 있는 길을 막았다 — 쿨다운이 NaN이거나 60초를 넘으면 되돌리고,
> `1/spd`가 Infinity가 되지 않게 하한을 두고, `dt`의 음수·NaN을 걸러내고,
> 그림 로딩이 8초를 넘으면 있는 것만으로 시작하게 했다.

### ③ 튜토리얼이 옛 게임을 설명하고 있었다

바뀐 규칙이 쪽지에 반영돼 있지 않았다.

| 쪽지 | 옛 설명 | 지금 |
|---|---|---|
| 💰 드랍 | "처치하면 그 자리에 골드가 떨어진다" | 기본 골드는 즉시 들어오고, 값나가는 것만 떨어진다 |
| 🏁 웨이브 종료 | "60초를 버텼다" · "후퇴하면 성벽이 깎인다" | 시계는 스폰 시간 · 후퇴하면 남은 무리가 상단으로 |
| 출발 안내 | "한 층은 60초입니다" | 시계는 몬스터가 나오는 시간 |

`TUTORIAL_VERSION`을 도입해 표시 기록을 판별로 갈랐다(`df_tip2_*`).
쪽지를 고쳐 쓰고 이 숫자를 올리면 이미 본 사람도 새 내용을 한 번 더 본다.

**캠프 › 기록 탭에 다시 보기를 넣었다** — 📖 처음부터 다시(기본 6장) · 🔁 쪽지 초기화.
예전에는 전체 초기화 말고는 다시 볼 길이 없었다.

### ④ 🎵 배경음

효과음과 같이 **WebAudio로 합성**한다. 음원 파일을 받으면 용량이 몇 MB 늘고 라이선스도
따로 챙겨야 하는데, 이 게임은 이미 소리를 전부 만들어 쓰고 있다.

곡은 셋 — 캠프(84 BPM, 잔잔) · 전투(132 BPM, 몰아침) · 심층(100 BPM, 어둡게).
`BGM.sync(gs, wm)`가 매 프레임 화면과 웨이브 상태를 보고 알아서 고르고,
같은 곡이면 아무 일도 하지 않는다.

구조는 룩어헤드 스케줄러다. 25ms마다 깨어나 앞으로 0.2초 안에 울릴 음만 예약한다 —
`setInterval`로 음을 직접 울리면 탭이 뒤로 갔을 때 박자가 무너진다.

캠프 › 기록 탭에서 배경음과 효과음을 따로 끌 수 있다.

### ⑤ 버튼을 키웠다

480 논리폭을 6인치 화면에 늘려도 24px 버튼은 손가락에 여전히 작았다.

| | 이전 | 지금 |
|---|---|---|
| 전투 컨트롤 (⏸·배속·음소거) | 34 × 24 | **44 × 32** |
| 자동 / 후퇴 | 76 × 24 | **92 · 104 × 32** |
| 컨트롤 바 높이 | 32 | **40** |
| ← 뒤로 | 50 × 22 | **64 × 30** |
| 상점 탭 | 66 × 20 | **80 × 28** |
| 건물 승급 | 140 × 22 | **152 × 28** |
| 강화 구매 · 매대 | 68 × 22 | **82 · 74 × 30** |
| 영웅 배치 | 높이 34 | **44** |
| 보관함 칸 | 5열 × 48 | **4열 × 58** |

넘치는 곳은 스크롤로 받는다. **캠프 기록 탭에도 스크롤을 붙였다** —
안내와 소리 항목이 늘면서 한 화면에 안 들어간다.

### ⑥ 🌊 쇄도 — 한 번에 몰려오는 순간

한두 마리씩 꾸준히 나오는 리듬만 있으면 60초가 평탄하다. 위험이 오르내려야
"지금 물러날까"가 판단거리가 된다.

| | 값 |
|---|---|
| 첫 쇄도 | 18초 ± |
| 그 뒤 간격 | 22초 ± 6 (흔들어서 초시계를 못 세게) |
| 경고 | 2초 — 테두리 점멸 + 로그 |
| 마릿수 | 6 + 층×0.22, 최대 14 |

쇄도는 동시 생존 상한(20)을 잠시 무시한다. 상한에 걸려 반만 나오면 쇄도로 안 읽힌다.

### ⑦ 하단을 비우면 그대로 상단으로 올라온다

> "하단 아레나를 그냥 후퇴하면 돈을 좀 못 버는 것 말고는 별다른 손해가 없는 것 같은데…
> 조기 후퇴를 하면 남은 웨이브가 한 번에 타워로 밀고 들어오는 형식을 추가해서
> 상하단을 모두 신경 쓸 수 있는 게임이 되면 좋겠어. (전멸 시에도)"

정확한 지적이다. 예전 후퇴 비용은 **성벽 HP 정액 차감**이었다 — 숫자가 조용히 깎일 뿐이라
"돈을 조금 덜 벌었네" 정도로 읽혔고, 상단이 하단보다 쉬운 것과 겹쳐 아레나를 일찍 접는 쪽이
언제나 편했다.

이제 하단을 비우면 **거기 있던 것들과 아직 나오지 않았던 몫의 일부가 그대로 상단 경로로 들어온다.**

| | 값 |
|---|---|
| 넘어가는 수 | 생존 몹 + 남은 스폰 시간 ÷ 스폰 간격 (최대 10) |
| 총 상한 | 18마리 |
| 체력 | ×0.72 — 아래에서 이미 싸웠으므로 |
| 입장 간격 | 0.55초 — 한 줄로 겹치지 않게 |
| 성벽 정액 피해 | **없앴다** |

전멸도 같다. 예전에는 아레나에 남은 몹이 **보이지 않는 DPS로 성벽을 갉았는데**,
화면 밖에서 숫자만 줄어드는 것이라 막을 방법도 없고 읽히지도 않았다.
이제는 그것들이 눈에 보이게 위로 올라오고, 타워로 막을 수 있다.

후퇴 버튼에 **지금 빼면 몇 마리가 올라오는지**를 띄운다 (`🛡 후퇴 ⬆18`, 10 이상이면 붉게).
누르기 전에 값을 알아야 판단이 된다.

실측: 6층에서 하단 생존 8 + 대기 10 = 18마리가 넘어왔고, 성벽 피해 0,
0 / 0.55 / 1.1 / 1.65초 간격으로 입장, 웨이브는 그것들을 다 정리해야 끝났다.

---

## v11.7 요약 — 보석은 깊이에만 붙는다 (기획서 v4.7)

게임 버전 **v0.7.1**.

### ① 들어가자마자 나가도 보석이 나왔다 (버그)

> "심연 모드에서 스테이지 시작하자마자 나가도 보석을 하나 주는 현상이 있어."

`calcSoulStones`에 바닥이 두 겹 깔려 있었다.

```js
const base = Math.max(1, endTerm + caveTerm + killTerm + recTerm);
return Math.max(1, Math.round(base * mult));
```

케이브 레벨이 최소 1이라 `caveTerm`이 항상 1을 얹었고, 거기에 기록 갱신 항목까지 붙었다.
**한 층도 못 넘긴 판이 자기 자신을 갱신한 것으로 쳐서** — `bestEndless`는 층을 넘겨야 갱신되는데
`tier = wave+1 = 1 > 0`은 언제나 참이라 — 매번 3을 더 받았다. 들어갔다 나오기만 반복하면
판당 2보석이 무한히 나왔다.

바닥을 걷어내고, **한 층도 못 넘겼으면 0**으로 잘랐다. 기록 갱신은 *판을 시작할 때의 기록*과 견준다.

### ② 되짚는 층은 1/10

> "처음 클리어하는 스테이지에는 보석을 줘도 될 것 같은데, 그 이후에 도달하는 스테이지는
> 보상을 낮춰야 할 것 같아. 이미 클리어한 스테이지는 ×0.1을 하는 식으로."

정확한 지적이다. 정액이면 **최고 기록이 40층인 사람에게도 1~10층을 빠르게 훑고 나오는 쪽이
41층을 노리는 것보다 시간당 이득이 크다** — 깊이가 점수인 게임에서 얕은 반복이 최적 전략이 된다.

```js
const ENDLESS_REPEAT_MULT = 0.10;
function endlessGemStepFor(tier, bestAtStart) {
  return endlessGemStep(tier) * (tier > bestAtStart ? 1 : ENDLESS_REPEAT_MULT);
}
```

기준은 **판을 시작할 때의 최고 기록**(`gs.runBestAtStart`)이다.
판이 도는 동안 갱신되는 `bestEndless`를 쓰면 매 층이 자기 자신을 갱신해서 전부 첫 돌파가 돼 버린다.
세이브에도 남긴다 — 중간에 껐다 켜도 기준이 흔들리면 안 된다.

### ③ 깎은 몫은 '새로 돌파한 층'으로 옮겼다

되짚기만 1/10로 깎으면 층 적립이 4분의 1로 준다. 총량을 줄이는 게 목적이 아니라
**어디서 버는지를 바꾸는 것**이 목적이므로, 그 몫을 새 깊이로 옮겼다.

```js
const ENDLESS_NEW_FLOOR_GEM   = 1.0;    // 새로 돌파한 층 한 층당
const ENDLESS_NEW_FLOOR_DEPTH = 0.06;   // 기존 기록 한 층당 가산
```

깊은 층은 한 층 내려가기가 더 어려우니 층당 몫도 기록에 비례해 커진다.
예전의 "최고 기록 갱신 = max(3, 층/4)" 정액 보너스를 이걸로 갈아치웠다.

| 기록 | 도달 | 층 적립 | 새 깊이 | 합계 | (예전) |
|---:|---:|---:|---:|---:|---:|
| 0 | 10 | 4 | 10 | **15** | 8 |
| 0 | 20 | 12 | 20 | **33** | 18 |
| 10 | 10 | 0 | 0 | **1** | 8 |
| 20 | 25 | 6 | 11 | **18** | 25 |
| 30 | 30 | 2 | 0 | **3** | 32 |
| 40 | 45 | 13 | 17 | **31** | 62 |
| 50 | 55 | 17 | 20 | **38** | 86 |

*(케이브 1만 더한 값 — 처치 항목은 별도)*

읽는 법은 하나다. **한 층이라도 더 내려가면 벌고, 제자리면 거의 못 번다.**
첫 하강은 전부 새 깊이라 오히려 예전보다 후하다 — 신규 플레이어가 손해 보지 않는다.

> **막힘 방지** — 기록을 못 깬 판도 처치 항목(`처치 ÷ 60`)은 그대로 들어온다.
> 깊은 판이면 500마리 안팎이라 8보석쯤 된다. 들어갔다 나오면 처치가 0이라 여전히 0이다 —
> 값이 붙는 것은 '들어간 것'이 아니라 '싸운 것'이다.

### ④ 내려가기 전에 알 수 있게

- **브리핑** — `★ 신기록 구간 / 💎 첫 돌파 — 제값` 또는 `최고 N층 / 💎 되짚는 층 ×0.1`.
  이 층의 실제 적립도 되짚기 배율을 반영해 표시한다 (`+0.84` vs `+0.04`).
- **층 전망 표** — `endlessGemTotalFor(t, best)`로 *내 기록 기준* 실제 수령액을 보여준다.
- **결과 정산** — `∞ 새 깊이` / `∞ 되짚은 층`으로 갈라 적는다.
  층 적립은 소수로 쌓이고 총합에서 한 번만 내림하므로, 나눠 적을 때 합이 총합과 어긋나지 않게 맞췄다.
- **포기 예상치** — 바닥을 없앴다. 0이면 `💎0 — 한 층이라도 넘어야 보석이 남습니다`.

---

## v11.6 요약 — 이모지를 걷어낸다 (기획서 v4.6)

게임 버전 **v0.7.0**. 받아온 픽셀 아트 세 묶음을 실제로 붙였다.
붙은 그림 **51장** — 상단 전선이 도형 판에서 밭과 성이 있는 전장으로 바뀌었다.

### ① 받은 것

| 묶음 | 내용 | 규격 |
|---|---|---|
| `1 Tiles` | 밭 타일 64종 + 조립본 | 32×32 · 팔레트 4색 |
| `Simple Tower Defense` | 타워 6 · 투사체 6 · 성 3색 | 16~18 × 23~35 · 성 208×38 |
| `free-field-enemies…` | 적 4종 × 3방향 × Walk/Attack/Death | 288×48 = 48px 6프레임 |

### ② 타일 — 밭 사이로 난 잔디 길

에셋을 만든 사람이 함께 넣어둔 `FieldsTilesetTest.png`가 답을 갖고 있었다.
**주황 밭 위에 초록 잔디 길**이 나 있는 그림이다. 처음엔 반대로(흙길·잔디밭) 깔려다가
64장을 픽셀 단위로 분류해 보고 방향을 바꿨다.

| | 게임 | 타일 |
|---|---|---|
| 빈 칸 (타워 놓는 곳) | 밭 | 순수 흙 변형 **20종** |
| 길 (적이 지나는 곳) | 잔디 길 | 순수 잔디 1종 |

한 종류로 63칸을 채우면 격자가 그대로 드러난다. 칸 좌표를 해시해서 20종 중 하나를 고르므로
매 프레임 같은 타일이 나오고(깜빡이지 않고) 반복도 눈에 띄지 않는다.

> **오토타일은 포기했다.** 64장을 8방향 잔디 여부로 분류해 보니 서로 다른 패턴이
> 14종뿐이었고 서쪽 변 타일이 아예 없었다. 완전한 47-블롭 세트가 아니라,
> 흙 변형이 대부분인 장식용 팩이다. 억지로 이어 붙이면 한쪽 면만 어색해진다.

아레나 바닥도 같은 밭 타일을 깔고 어둡게 덮었다. 같은 세계인데 아래쪽만 검은 판이면
두 전선이 딴 게임처럼 보인다. 바닥은 변하지 않으므로 오프스크린에 한 번 구워 둔다 —
매 프레임 108장을 다시 깔 이유가 없다.

### ③ 타워 — 1:1로 맞아떨어졌다

| 게임 | 에셋 | 원본 |
|---|---|---|
| 🏹 화살탑 | `archer` | 16×23 |
| ❄️ 서리탑 | `ice_wizard` | 16×26 |
| 💣 대포탑 | `cannon` | 18×30 |
| 🎯 저격탑 | `crossbow` | 18×33 |
| ⚡ 번개탑 | `lightning_tower` | 18×35 |

비싼 타워일수록 원본이 크다. 비율 그대로 2.2배로 세우면 칸(53×50) 위로 솟고,
값이 비쌀수록 높아 보인다. 발밑에 타원 그림자를 깔아 칸에 붙게 했다.

성(`spr_castle`)은 52×38 4프레임 **깃발 애니메이션**이다. 기지 칸에 세웠다.
그 위를 덮던 보라색 "🏰마을" 힌트는 테두리 펄스로 바꿨다 — 덮으면 성이 안 보인다.

### ④ 적 — 그림 넷으로 열넷

애니메이션이 필요해졌다. `SPRITE_ACTORS`로 **방향 × 동작 × 프레임**을 다룬다.

- 방향은 진행 방향에서 뽑는다. 마지막으로 그린 자리와 비교하므로 **이동 코드를 건드리지 않는다.**
  옆으로 갈 때는 `S`를 뒤집고, 위아래는 `U`/`D`.
- 프레임은 전역 시계 + 개체 id 위상에서 계산한다. **개체에 애니메이션 상태를 들고 있지 않아도 된다.**
- 48px 프레임 안에서 캐릭터는 13~22px뿐이라 `foot`(발이 닿는 y)과 `head`(그림이 시작하는 y)를
  배우마다 적어 뒀다. 이게 없으면 체력바가 허공에 뜨고 발이 땅에서 떨어진다.

그림은 넷(슬라임·오크·늑대·벌)인데 몬스터는 열넷이다. **크기와 색**으로 갈라 쓴다.

| 게임 | 배우 | 크기 | 색 |
|---|---|---|---|
| 고블린 | 슬라임 | 1.00 | — |
| 늑대 · 광견 | 늑대 | 0.95~1.00 | — |
| 오크 | 오크 | 1.00 | — |
| 강철오크 | 오크 | 1.05 | 회색 |
| 던전보스 | 오크 | 1.15 | 붉게 |
| 현상수배 | 오크 | 1.05 | 금색 |
| 박쥐 · 비룡 | 벌 | 0.95 / 1.10 | 보라 |
| 다크아처 · 오우거 | 오크 | 1.00 / 1.10 | 자주 / 갈색 |

색은 **곱하기 후 원래 투명도 복원**으로 입힌다 — 명암이 그대로 남는다.
파일을 복사해 두는 대신 처음 쓸 때 한 번만 계산해 캐시에 남긴다.
피격 순간의 흰 섬광과 감속의 파란 기운도 같은 방식으로 얹었다.

### ⑤ 그 밖에

- **투사체 5종** — 날아가는 방향으로 돌려 그린다.
- **보간 끄기** — 픽셀 아트는 확대할 때 보간하면 죄다 뭉개진다. 그리기 직전에만 끄고 되돌린다.
- **체력바·등급 표시** — 그림의 실제 머리 꼭대기 위로 옮겼다. 원 기준 좌표를 그대로 두면
  프레임 여백만큼 허공에 뜬다.
- **성능** — 타워 40개 + 만원 웨이브에서 60fps 유지.
- **폴백은 그대로** — 매니페스트를 비우면 요청 0건에 예전 화면 그대로다. 실측으로 확인했다.

### ⑥ 아직 이모지인 것

아레나 **아군 8종과 영웅 3종**. 몬스터만 픽셀 아트라 아레나에서 편이 갈려 보인다.
`unit.<아이디>` · `hero.<각인>` 이름으로 코드는 이미 찾고 있으니 파일만 넣으면 된다.

---

## v11.5 요약 — 그림이 들어갈 자리를 판다 (기획서 v4.5)

게임 버전 **v0.6.1**. 새 파일 `js/sprites.js`와 `assets/images/manifest.js`.
아트 에셋 목록(`docs/아트_에셋_목록.md`)의 "코드가 먼저 할 일" 여섯 가지 중 넷을 구현했다.

### ① 규칙 하나 — 없으면 없는 대로 그린다

그림이 다 모일 때까지 기다려야 하는 구조여서는 안 된다. 한 장씩 받아 붙이는 동안에도
게임은 계속 돌아가야 한다.

```js
const key = tileSpriteKey(...);
if (!(key && Sprites.draw(ctx, key, x, y, CELL_W, CELL_H))) {
  ...지금까지의 색 사각형...
}
```

`Sprites.draw()`는 이미지가 없으면 아무것도 그리지 않고 `false`를 돌려준다.
호출한 쪽은 그 자리에서 예전 코드로 떨어진다. 타일 63칸이 각자 판단하므로
`ground.png` 한 장만 넣어도 그 칸부터 바뀐다.

### ② 매니페스트가 진입점 — 비어 있으면 요청이 0건

`assets/images/manifest.js`가 읽을 파일을 정한다. 비어 있으면 네트워크 요청이
한 건도 안 나가고 로딩 화면조차 뜨지 않는다. **아무것도 안 넣은 상태가 기본값**이다.

`self.SPRITE_FILES`로 선언해서 서비스 워커가 같은 파일을 `importScripts`로 읽는다 —
캐시 목록을 두 군데 적으면 반드시 어긋난다.

`SPRITE_AUTOLOAD = true`로 두면 매니페스트를 안 켜도 기본 경로를 전부 시도한다
(없는 파일마다 404 한 줄을 감수하는 대신).

### ③ 이름이 아래로 떨어진다

| 요청 | 없으면 |
|---|---|
| `tower.arrow.3` (Lv5) | → `.2` → `.1` |
| `tile.path_corner` | → `tile.path` → `tile.ground` |
| `tile.start` | → `tile.path` → `tile.ground` |

타워 한 종류에 그림 **한 장(`_1`)** 만 넣어도 다섯 레벨 전부에서 쓰인다.
전부 채워야 뭐라도 보이는 구조면 시작을 못 한다.

### ④ 아레나 몸집 — 아트보다 먼저 고쳐야 했던 것

아군 반지름이 **7.5px**였다. 이모지는 그 크기에서도 실루엣이 읽히지만
지름 15px짜리 점에 캐릭터를 그려 넣을 수는 없다.

| | 이전 | 지금 |
|---|---|---|
| 몸집 배율 | — | **×1.85** (`ARENA_BODY_SCALE`) |
| 아군 반지름 | 7.5 | **13.9** |
| 영웅 · 방패병 | 9 | 16.7 |
| 동시 생존 상한 | 28 | **20** |
| 대형 간격 | 24 / 26 | **32 / 34** |

근접 사거리는 대상의 *표면*까지로 잰다(`dist − target.radius`). 그래서 몸이 커져도
**자기 반지름보다 사거리가 길기만 하면** 계속 닿는다. 가장 빠듯한 것이
오우거(19 vs 26)와 보스(20.9 vs 30)이고 둘 다 여유가 있다 — 실측으로 확인했다.

대형 간격을 같이 벌리지 않으면 지름 28px짜리가 24px 간격으로 서서
서로 밀어내느라 자리에 못 선다. 수정 후 슬롯 이탈 거리는 2~3px.

### ⑤ 그림은 발밑 기준

`Sprites.drawFoot(ctx, key, cx, footY, w, h)` — 바닥선이 맞아야 앞뒤가 읽힌다.
타워는 48×56으로 그려서 칸(53×50) 위로 솟고, 아레나 개체는 반지름의 2.1배 × 2.7배다.
충돌원은 발밑에 남고 그림만 그 위로 선다.

### ⑥ 임시 그림 22장

타일 7 + 타워 15를 코드로 만들어 넣어뒀다. 규격이 맞는지 눈으로 확인하고,
받아온 그림을 **같은 이름으로 덮어쓰면** 그대로 바뀐다.
넣는 법은 `assets/images/README.md`에 있다.

---

## v11.4 요약 — 영웅에게 칸을 준다 (기획서 v4.4)

게임 버전 **v0.6.0**. 새 파일 `js/hero.js`(약 380줄)가 영웅의 장비·스킬을 통째로 맡는다.

### ① 전투 탭도 스크롤된다

준비 화면(브리핑)은 480×390 패널에 그대로 그려졌다. 심층 이벤트와 완주 보너스가
붙으면서 아래 줄이 잘렸고, 그 안에 **🏰마을 버튼**이 있었다.

마을 탭이 쓰던 드래그·휠 배관을 그대로 붙였다. `renderBriefing(ctx, gs, top)`이
`BATTLE_Y - scroll`을 받고, 내부 좌표는 전부 `TOP+`로 옮겼다. 배경 두 장만
화면에 고정해 둔다 — 같이 움직이면 패널 밖이 비어 보인다.

측정: 20층 기준 44px 스크롤, 마을 버튼이 y=714에서 눌린다.

### ② 장비는 정해진 칸에만

> "지금은 영웅 상점에서 아무렇게나 사서 장착할 수 있는데, 이제는 무기 갑옷 헬멧 바지
> 신발 악세사리 2개 이렇게만 장착할 수 있게 하고..."

이전에는 산 장비가 **사는 즉시 영구히** 붙었다. 고를 것이 없었다 — 비싼 걸 사면 그게 정답.

| | 이전 | 지금 |
|---|---|---|
| 칸 | 없음 (무제한 누적) | 무기·투구·갑옷·각반·신발·악세 ×2 = **7칸** |
| 구매 | 즉시 장착 · 영구 | **🎒보관함**으로 · 장착은 따로 |
| 해제 | 불가 | 언제든 |
| 도감 | 12종 (칸 개념 없음) | **21종** — 칸마다 3~4등급 |

아이템은 임의의 `apply(b)` 클로저 대신 **스탯 블록**(`{atk:8}`)으로 값을 준다.
그래야 "끼우면 얼마나 바뀌는지"를 계산할 수 있다.

스탯 키 10종 — `atk · hp · def · regen · aspd · range · skill · crit · gold · exp`.
새 보너스 키 `heroHpFlat · heroSpdMult · heroRangeMult · heroSkillMult`를 더해
각인 배율과 곱해 쓴다.

### ③ 스탯창 — 끼우기 전에 안다

보관함에서 하나 고르면 오른쪽 스탯창이 **지금 → 바꾼 뒤**를 나란히 보여준다.

미리보기는 실제로 끼워보고 되돌리는 방식이다 (`heroStatPreview`). 보너스 재계산은
매 웨이브 도는 연산이라 한 번 더 도는 비용은 무시할 수 있고, 근사치가 아니라 **정확한 값**이 나온다.

### ④ 🔮 영웅 스킬 — 레벨이 칸을 열고, 굴림이 값을 정한다

> "레벨이 올라가면 스킬칸이 생겨서 스킬을 배울 수 있게 할 건데 스킬은 랜덤성을 부여해보자."

| 요소 | 값 |
|---|---|
| 칸 개방 | 영웅 Lv. **3 · 7 · 12 · 18** (최대 4칸) |
| 판매 조건 | 🏪영웅 상점 **Lv.3** 이상 |
| 스킬 종류 | 10종 (일반 3 · 희귀 5 · 영웅 2) |
| 굴림값 | 0.70 ~ 1.40 · 등급별 하한 (일반 0.70 / 희귀 0.85 / 영웅 1.00) |
| 값 | `기본가 × (0.6 + 굴림×0.55)` — 좋은 굴림은 비싸다 |

같은 '광폭'이라도 0.72짜리와 1.34짜리는 다른 물건이다. ★~★★★로 굴림을 먼저 읽히게 했다.

### ⑤ 영웅 상점의 강화 트랙이 다시 잡힌다 (버그)

`renderBuildingScreen`이 `heroShop`이면 매대를 그리고 **바로 return**했다.
그래서 이 건물의 강화 트랙(👑영웅 단련 · 📖전투 교본 · ✨영웅 각성 · 🎖️지휘 오라 ...)과
레벨 승급 버튼에 **아예 손이 닿지 않았다**. 정의만 있고 살 수 없는 강화가 6종.

🛒매대 / ⚒️강화 두 탭으로 나눴다. 매대도 전체 화면(SCR_TOP=92)으로 올리고 스크롤을 달았다.

### ⑥ 각인은 보석으로 연다

> "캠프에서 지금 영웅을 3개 선택할 수 있게 해놨는데 이거도 보석으로 해금하는 방식으로 가자."

| 각인 | 해금 |
|---|---|
| ⚔️ 검성 | 처음부터 |
| 🛡️ 수호자 | 💎15 |
| 🔥 술사 | 💎25 |

셋 다 열려 있으면 고르는 데 값이 없다. 심연에서 모은 보석이 "다음 영웅"이 되게 했다.

### ⑦ 옛 세이브

`normalizeHeroGear(saved, legacyEquipped)`가 둘 다 받는다. 옛 세이브의 장착 배열은
이름이 바뀐 12종을 새 아이디로 옮기고, 들어갈 수 있는 칸에 다시 끼운다.
uid 카운터는 실린 값 위로 올려 새로 산 물건이 옛 uid와 부딪히지 않게 한다.

---

## v11.3 요약 — 시간은 스폰까지, 끝은 판이 비어야 (기획서 v4.3)

게임 버전 **v0.5.1**.

### ① 타이머의 뜻을 바꿨다

> "시간이 다 지나갈 때까지 웨이브가 타워로 끝까지 들어가지 못하는 현상이 생겼어.
> 근데 생각해보니 타워 중에 적을 느려지게 하는 타워도 있을 텐데, 그러면 최적의 세팅을 해도
> 끝까지 들어가지 못할 테니 상단에 남은 몹이 있으면 시간이 지나도 끝까지 진행하도록 해."

정확한 지적이다. **서리탑으로 느리게 만들수록 손해를 보는 규칙**이었다 —
잘 막을수록 마지막 몹이 기지에 닿기 전에 웨이브가 끝나 처치 보상도 못 챙겼다.

| | 이전 | 지금 |
|---|---|---|
| 60초의 뜻 | 웨이브 길이 | **몹이 나오는 시간** |
| 종료 | 타이머 0 | **양쪽 판이 다 비어야** |
| 상단 | 남아도 잘림 | 다 죽거나 기지에 닿을 때까지 |
| 하단 | 남아도 잘림 | 다 죽거나, 후퇴하거나, 전멸할 때까지 |

스폰 시간이 끝나면 상단·하단 모두 새로 나오지 않는다.
UI도 ⏱ 카운트다운에서 **🧹 정리 중 N**(남은 마릿수)으로 바뀐다.

안 죽는 조합에 걸려도 영원히 끌지 않도록 `WAVE_CLEANUP_MAX = 90`초 상한을 뒀다.
거기 걸리면 남은 상단 몹은 기지에 닿은 것으로, 하단 몹은 물러난 것으로 친다.

실측 (화살 7 + 서리 7, 3층): 스폰 60초 + **정리 9초** = 69초.
60초 시점에 상단 0 · 하단 12기가 남아 있었고, 9초에 걸쳐 다 정리한 뒤 종료 — 기지 100HP 유지.

### ② 1-1이 화살탑 하나로 막히지 않았다

v4.2에서 도착 간격을 1.3초로 좁힌 것이 **첫 웨이브에까지 적용**됐다.
화살탑 한 기로 손도 못 대는 물량이 몰려왔다.

훈련 첫 세 웨이브만 성기게 잡는다 (`OPENING_TARGET_GAPS = [4.5, 3.2, 2.2]`).
네 번째부터는 곧장 1.3초다. **심연은 첫 층부터 본편이므로 예외 없이 1.3초.**

### ③ 1-1 하단은 영웅 하나로

> "1-1은 하단의 몹 양을 좀 더 줄여서 적은 돈을 얻지만 영웅 하나로 클리어할 수 있는 정도로."

웨이브 정의에 `spawnMult`(아레나 스폰 간격 배수)를 붙였다 — 1-1은 4.2배, 1-2는 2.6배, 1-3은 1.7배.

실측 (화살탑 1기 + 영웅만):

| | 결과 |
|---|---|
| 기지 | **100HP 유지** (한 기로 상단이 막힌다) |
| 영웅 | 80/80 **무상처 생존** |
| 처치 | 17마리 · 수입 36골드 |
| 소요 | 63초 (스폰 60 + 정리 3) |

### 훈련 전체 곡선

| 웨이브 | 1 | 2 | 3 | 4 | 5 | 6 |
|-------|---|---|---|---|---|---|
| 시작 골드 | 10 | 77 | 136 | 199 | 189 | 308 |
| 기지 HP | 100 | 100 | 100 | 100 | 73 | 46 |

1-1은 안전하고, 뒤로 갈수록 기지가 깎인다 — 의도한 모양이다. 6웨이브 완주.
프레임 16.7ms 중앙값.

---

## v11.2 요약 — 상단은 빽빽하게, 하단은 주울 값어치만 (기획서 v4.2)

게임 버전 **v0.5.0**.

### ① 상단이 아직도 띄엄띄엄하다

v4.0에서 도착 간격을 2.2초로 잡았는데 "긴장감이 없다"는 보고가 왔다.
**1.3초**로 좁혔다. 마릿수가 그만큼 늘고, 마리당 보상은
`buildSpawnPlan`의 `rewardMult`가 자동으로 낮춘다 — 보이는 것만 바뀌고 버는 돈은 그대로다.

실측 (1웨이브, 화살탑 2기 + 궁수 + 영웅):

| | 상단이 빈 시간 | 경로 위 개체 수 |
|---|---|---|
| v4.0 (2.2초) | 6~11% | 1~2기 |
| **지금 (1.3초)** | **0%** | 1 → 13기까지 차오름 |

### ② 하단 드랍 — 자동 수거 + 값나가는 것만 줍기

60초 내내 동전을 주우러 다니는 것은 조작이 아니라 잡일이다.
**기본 골드는 처치 즉시 들어온다.** 대신 처치의 7.5%에서 값나가는 것이 떨어지고,
그것만 주우러 간다 — 움직일 이유는 남기되 움직임이 의무가 되지 않게.

| 드랍 | 비중 | 효과 |
|-----|-----|-----|
| 💰 금화 더미 | 30 | 기본 골드의 5~10배 |
| ✨ 전투 기록 | 22 | 영웅 EXP 뭉치 |
| ❤️ 응급 치료 | 16 | 부대 전원 최대 HP 15% 회복 |
| 🔥 분노 | 10 | 공격력 ×1.6 (9초) |
| 💨 질풍 | 10 | 공격속도 ×1.6 (9초) |
| 🛡️ 수호 | 6 | 받는 피해 ×0.55 (9초) |

보스는 +50%p, 정예는 +22%p 확률이 붙는다.
버프는 아레나 좌상단에 남은 시간이 뜬다.

**골드 보정.** 자동 수거로 흘리는 골드가 없어져서(예전에는 못 주운 드랍이 사라졌다)
같은 몹으로 훨씬 많이 벌게 됐다 — 1-2 웨이브 5 시작 골드가 400 → **1,001**.
`ARENA_GOLD_SCALE = 0.58`로 마리당 값을 낮춰 되돌렸다.

### ③ 버그 — 수동으로 끝냈는데 다음 층에서 표시만 수동

`startArena()`가 `rally`만 지우고 `mode`는 그대로 뒀다. 그래서
**표시는 수동, 행동은 자동(rally가 없으니 제자리 사수), 배속만 2배로 묶인** 상태가 됐다.
웨이브가 시작되면 자동으로 돌아가게 하고, 배속 강등도 함께 푼다.
플레이어가 고른 배속은 `_speedPref`에 따로 기억한다 — 이게 없으면
한 번 수동을 만졌다가 자동으로 돌아와도 2배속에 갇힌다.

실측: 수동 중 `{mode:manual, speed:2}` → 다음 웨이브 `{mode:auto, speed:3, rally:없음}`.

### ④ ⚔️ 아레나 정예 소환 — 상단 현상수배의 짝

마을에서 예약하면 웨이브 시작 6초 뒤 아레나에 강한 한 마리가 나온다.
잡으면 보석과 큰 골드, 못 잡으면 부대가 죽는다.

- 상단 현상수배는 **경로를 막을 화력이 있는가**를 묻고
- 하단 정예는 **부대가 정면으로 이겨낼 수 있는가**를 묻는다

그 층 풀에서 가장 무거운 종류를 골라 HP ×6(최소 200) · ATK ×2로 부풀린다.
기회는 4층마다 하나씩 늘어난다(현상수배는 3층마다).

### ⑤ 보석 재조정 — 경로가 셋이 됐다

소환 갈래가 둘이 되면서 보석이 두 배로 들어올 참이었다.
층당 적립과 관문 보상을 낮추고, 소환 한 마리당 보석 증가도 완만하게 했다.

| | 이전 | 지금 |
|---|---|---|
| 층당 적립 | 0.35 + 0.055/층 | **0.24 + 0.040/층** |
| 관문 최초 돌파 | 6 + 4/관문 | **5 + 3/관문** |
| 소환 보석 | 1 + n/2 | **1 + n/3** (양쪽 공통) |

45층까지 모으는 총 보석: **185 → 175**. 새 획득 경로를 하나 더 열면서 총량은 그대로다.
구성은 깊이 100 · 소환 75로, 여전히 깊이가 앞선다 — 기록이 곧 깊이인 게임이므로.

### 실측

드랍의 영향만 따로 보려고 같은 편성으로 12층까지 A/B를 돌렸다.

| | 층별 기지 HP (1→5층) | 전멸 | 누적 수입 |
|---|---|---|---|
| 드랍 ON | 100 86 59 31 2 | 5회 | 226 |
| 드랍 OFF | 100 73 46 31 13 | 5회 | 217 |

거의 같다 — 새 드랍 체계는 난이도를 흔들지 않는다.

**하강 깊이는 이번에 다시 재지 못했다.** 상단 마릿수가 4배 가까이 늘면서
한 층을 시뮬레이션하는 실시간이 길어져, 스크립트 하강이 죽기 전에
시간 예산(3배속 17분)에 먼저 걸린다 — 초심자·만렙 둘 다 18층에서 기지 HP를 온전히
남긴 채 잘렸다(`사망 false`). 곡선 판단은 위 A/B에 근거한다.

프레임 16.7ms 중앙값.

---

## v11.1 요약 — 건물마다 하는 일을 하나씩 (기획서 v4.1)

게임 버전 **v0.4.3**.

> "여관에 체력증가, 병영에 체력증가는 겹치는 것 같아. 여관의 체력증가는 삭제해줘.
> 마을의 병기연구는 삭제해줘 — 업그레이드는 건물을 짓고 거기서 하는 게 맞는 것 같아.
> 여관은 용병과 영웅의 턴이 끝난 뒤의 회복 및 특수용병의 강화·확률 업그레이드만."

### 겹치던 것 — 여관 '뜨거운 식사'

병영의 **💪 체력 단련**과 여관의 **🍲 뜨거운 식사**가 둘 다 `unitHp`를 올리고 있었다.
값이 같으면 어느 쪽을 올릴지가 **선택이 아니라 계산**이 된다 — 싼 쪽을 사면 그만이다.
여관에서 뺐다. 이제 `unitHp`의 출처는 병영뿐이다.

### 여관을 여관이 하는 일로 좁혔다

| 트랙 | 열림 | 하는 일 |
|-----|-----|--------|
| 🛏️ 따뜻한 잠자리 | Lv.1 | 웨이브 후 회복 +3%/단계 |
| 🍺 소문난 주점 | Lv.1 | 특수 용병 등장 확률 +6%/단계 |
| 📜 **명성** (신규) | Lv.2 | **특수 용병 능력치 +8%/단계** |
| 🚪 별관 증축 | Lv.3 | 특수 용병 전용 슬롯 +1 |
| 🍷 영웅 대접 | Lv.5 | 웨이브 후 영웅 완전 회복 |
| ♾️ 끝없는 환대 | Lv.10 | 회복 + 특수 용병 능력치 |

빠진 것: 🍲 뜨거운 식사(병영과 중복), 🧴 회복 물약(전투 중 자연 회복 — 웨이브 후 회복이 아니다).
그 자리에 요청하신 **특수 용병 강화**를 넣었다. 지금까지 특수 용병은
등장 확률과 슬롯만 있고 강해질 방법이 없었다.

`BONUSES.specialUnitMult`는 `UNIT_TYPES[id].special`인 유닛의 ATK · HP · DEF · 스킬에만 곱한다.
실측 — 명성 5단계(×1.48)에서 🗡️도적 ATK 19→28 · HP 88→130, 일반 궁수는 그대로.

### 병기 연구 삭제

마을 메인 화면에 있던 ⚗️ 병기 연구(타워 +2 / 아군 +4, 비용 ×1.38)를 없앴다.
같은 일을 공방의 **날붙이 연마**와 병영의 **전투 훈련**이 이미 하고 있었고,
건물 밖에 강화가 하나 떠 있으니 "강화는 건물에서 한다"는 규칙이 흐려졌다.
무한 골드 사용처 역할은 각 건물의 ♾️ 트랙이 이미 맡고 있다.

상수 · 세이브 필드 · 탭 핸들러 · 보너스 적용까지 함께 걷어냈다.
기존 세이브의 `research` 값은 무시된다.

### 곡선

무한 첫 판 **11층** · 만렙 **31층** — 둘 다 직전과 같다.
연구가 먹던 골드가 건물 트랙으로 흘러가 값이 상쇄됐다. 프레임 16.7ms.

---

## v11.0 요약 — 플레이 보고 6건 (기획서 v4.0)

게임 버전 **v0.4.2**. 실제 플레이에서 나온 보고 다섯 건 + 그 과정에서 찾은 것 하나.

### ① 화면 아래가 잘려 안 보인다

출전준비 탭의 [전투력 요약]이 화면 밖으로 밀려 있었다. 스크롤이 없었을 뿐 아니라,
코드가 **자리가 없으면 아예 그리지 않고 있었다**:

```
if (y < CH-40) { … }              // 요약 전체
roundRect(ctx,6,y,CW-12,CH-y-10)  // 높이를 "화면 잔여분"으로 잡음
```

편성이 늘면 요약이 통째로 사라졌다. 잘라내는 대신 **넘기도록** 바꿨다.

- 마을 3탭 본문 전체가 스크롤된다 (`gs.ui.pageScroll`). 드래그·휠 모두 기존 배관을 쓴다.
- 상자 높이를 내용에 맞춰 고정했다 (`CH-y` 계산 제거).
- 렌더러에 **스크롤이 반영된 startY**를 준다 — 그림과 버튼 좌표가 함께 움직여
  탭 판정이 어긋나지 않는다. 헤더를 침범하지 않도록 클립을 건다.
- 오른쪽 스크롤 막대 + 아래쪽 **▼ 아래로 밀어서 더 보기** 그림자.
  더 볼 것이 있다는 사실 자체를 알려야 한다.

측정: 만렙 편성 기준 출전준비 탭 본문이 868px, 보이는 높이가 708px — 78px가 잘려 있었다.

### ② 용병 슬롯이 다음 판에 되돌아간다 — 실은 모든 강화 카드가 그랬다

보고는 "슬롯이 한 번만 늘고 다음 판에 줄어든다"였는데, 파보니 훨씬 컸다.

`reapplyAllBonuses()`는 `resetBonuses()`로 시작해 스킬 트리 · 서약 · 연구 · 마을 강화 ·
각인을 차례로 얹는다. **이번 판에 집은 강화 카드(`gs.activeUpgrades`)만 빠져 있었다.**
그리고 `wm.startWave()`가 웨이브마다 `reapplyAllBonuses()`를 부른다.

> 즉 강화 카드는 **집은 그 웨이브에만 효과가 있고 다음 웨이브에 전부 사라졌다.**
> 슬롯은 화면에 숫자로 떠서 눈에 띄었을 뿐이다.

`applyRunUpgrades(gs)`를 추가했다. 되풀이하면 안 되는 카드를 갈랐다.

| 표시 | 뜻 | 예 |
|-----|---|---|
| `once` | 집는 순간만 | 🏰 성벽 보수(+20 회복), 💰 황금 손길(+15골드) |
| `persist` | 지속분만 되살림 | 🏰 견고한 기지 (최대HP +20은 유지, 즉시 회복은 한 번) |
| (없음) | `apply` 그대로 재생 | 나머지 전부 |

실측: 슬롯 4 → 5, 재계산 후에도 5. 타워 공격력 +3 유지. 골드 카드는 25에서 안 늘어남.
겸사겸사 네 군데에 흩어져 있던(그중 둘만 층 이벤트를 반영하던) 슬롯 계산을
`recalcMaxSlots(gs)` 하나로 모았다.

### ③ 튜토리얼 보석이 너무 많다

1-2쯤에서 17보석이 모여 **영웅 스킬을 전부 찍을 수 있었다** — 본편을 시작하기도 전에
영구 성장이 끝난다. 스테이지별 지급(1개씩, 마지막 2개)을 없애고,
정산도 도달 웨이브·처치·케이브 합산에서 **고정값**으로 바꿨다.

| | 이전 | 지금 |
|---|---|---|
| 스테이지 클리어 | 개당 1~2 (총 11) | **0** |
| 완주 정산 | 웨이브×0.5 + 케이브 + 처치 + 신기록 | **2** |
| 중단 정산 | 위와 동일 | **1** |

훈련은 익히는 곳이지 버는 곳이 아니다.

### ④ 훈련 30웨이브 → 6웨이브

30웨이브는 30분이 넘는다. 본편에 닿기 전에 지친다.
`TRAINING_WAVES = 6`으로 줄였다 — 실측 **약 6분**(마을을 둘러보면 8~10분).
`WAVE_DEFS`의 나머지 항목은 남겼다: 곡선의 참고이자 늘리고 싶을 때의 여유다.

### ⑤ "무한 모드"라는 이름

본편인데 서브 콘텐츠처럼 들린다는 지적. **∞ 심연**으로 바꿨다 —
이미 쓰고 있던 어휘(하강 · 층 · 심층 · 관문)와 이어진다.
건물의 **♾️ 무한 강화**는 다른 것이라 그대로 뒀다.

### ⑥ 덤 — 병력 HP가 `276.40000000000003`

고용 카드의 스탯 미리보기가 반올림 없이 그대로 찍히고 있었다.
실제 유닛(`applyUnitStats`)은 반올림하므로 표시만 어긋난 것.
미리보기도 반올림하고, 빠져 있던 서약 배율을 함께 반영했다.

---

## v10.9 요약 — 전선마다 다른 것을 준다 (기획서 v3.9)

게임 버전 **v0.4.1**.

> "상단 타워 몬스터의 처치 골드는 진짜 소량만 주도록 조절하고, 대신 하단 아레나에서
> 영웅이 전투를 할 때도 소량의 경험치를 얻을 수 있게 해줘. 어느 쪽의 비중이 더 높냐에
> 따라 배치하지만 반대의 보너스도 얻을 수 있게."

### 무엇이 문제였나

영웅 배치는 두 갈래인데 **한쪽이 EXP를 독점하고 있었다.**
상단에 세우면 EXP가 들어오고, 하단에 세우면 **0**이었다.
그래서 배치가 "이번 층에 어디가 급한가"가 아니라
"영웅을 키울 거냐 말 거냐"가 됐다 — 답이 하나뿐인 선택은 선택이 아니다.

골드 쪽도 어정쩡했다. 실측하니 상단이 전체 수입의 **23%**였다 —
막는 곳이면서 버는 곳이기도 한 애매한 위치.

### 지금

| | 골드 | 영웅 EXP |
|---|---|---|
| **상단** (타워디펜스) | 극소량 (전체의 7~8%) | 배치 시 직접 1.00 · 타워 처치 0.40 |
| **하단** (아레나) | 주 수입 | 배치 시 0.40 |
| 반대쪽 전선 | — | **0.15** ← "반대의 보너스" |

`grantHeroExp(baseReward, side, direct)` 하나로 모았다.
영웅이 서 있는 전선이면 많이, 반대쪽이면 소량 — 어느 쪽에 서든 양쪽에서 들어온다.

### 헛디딘 곳 — 올림이 축소를 먹었다

`DEFENSE_GOLD_SCALE = 0.25`를 넣고 재보니 상단 비중이 23% → **14%**밖에 안 줄었다.
`Math.max(1, ...)`가 걸려 있어서, 이미 1골드짜리가 된 적은 아무리 곱해도 1골드였다.
(v3.8의 `rewardMult`가 초반 적 보상을 이미 1까지 내려둔 상태였다.)

소수점을 모아 두었다가 1이 넘을 때만 지급하도록 바꿨다(`_defGoldFrac`).
0골드 처치에는 골드 플로티를 띄우지 않는다 — 상단은 원래 푼돈이다.

### 실측 — 배치별 수확 (1-1~ 동일 드라이버)

| 배치 | 웨이브당 EXP | 웨이브당 골드 | 상단 골드 비중 |
|-----|------------|-------------|--------------|
| 👑 상단 | **70** | 116 | 8% |
| 👑 하단 | 44 | **136** | 7% |

상단은 EXP가 59% 많고, 하단은 골드가 많으면서 더 오래 버틴다(웨이브 10 vs 6).
하단 배치의 EXP는 **0에서 44로** 올라왔다 — 이게 "반대의 보너스"다.

`ARENA_EXP_BASE`는 두 배치를 실측으로 맞춰 0.70에 놓았다.
1.40으로 잡았을 때는 하단이 오히려 EXP까지 앞서서, 문제가 반대 방향으로 뒤집혔다.

편성 화면의 배치 버튼 아래에 **EXP 많이 · 골드 조금** / **골드 많이 · EXP 조금**을 적었다.

### 곡선

무한 첫 판 **12층** (직전 11층) · 훈련 드라이버 웨이브 8 (동일) ·
1-1 웨이브 2 시작 골드 32 · 프레임 16.7ms.

---

## v10.8 요약 — 두 전선의 길이를 맞춤 (기획서 v3.8)

게임 버전 **v0.4.0**.

> "타워디펜스는 금방 끝나고 아래쪽 아레나는 60초 동안 진행되는 게 밸런스가 안 맞는 것 같아.
> 마지막 몹이 성까지 닿는 시간이 60초가 되도록 수정."

맞는 지적이었고, 재보니 생각보다 심했다.

| 웨이브 | 1 | 3 | 6 | 9 | 12 | 21 | 30 |
|-------|---|---|---|---|----|----|----|
| 마지막 적 도착 (이전) | 27.7초 | 31.3초 | 34.7초 | 46.6초 | 62.1초 | 74.1초 | 107.8초 |

초반은 **28초에 상단이 텅 비고 남은 32초를 아레나만** 돌았고,
후반은 반대로 넘쳐서 웨이브가 끝나도 절반이 경로 위에 남았다.
웨이브 정의에 적힌 `interval`을 그대로 쓴 결과다 — 그 숫자는
"기지에 언제 닿는가"와 아무 상관이 없었다.

### 스폰을 시각이 아니라 도착으로 짠다

`buildSpawnPlan()`이 웨이브마다 일정을 새로 계산한다. 웨이브 정의에서는
**구성과 마릿수만** 읽고, `interval`은 더 이상 쓰지 않는다.

1. **도보 시간을 먼저 잰다** — `pathTravelTime()`이 현재 경로 길이를
   그 종류의 실효 속도(무한 배율·서약·층 이벤트 포함)로 나눈다.
   경로는 층마다 바뀌므로 경로 배열을 키로 캐시한다.
2. **마지막 스폰 = 웨이브 길이 − 도보 시간.** 마지막 한 마리가 60초에 기지에 닿는다.
3. **뜸하면 채운다** — 스폰 창을 마릿수로 나눈 간격이 `SPAWN_TARGET_GAP`(2.2초)보다
   크면 구성 비율을 지킨 채 전체 마릿수를 늘린다.
4. **꼬리를 메운다** — 빠른 적(도보가 웨이브의 절반 미만)은 웨이브의 80%까지 계속 내보낸다.
   2번만 지키면 39초에 스폰이 끊기고 타워가 남은 것을 다 잡아버려
   마지막 15초가 다시 텅 빈다. 늦게 나온 놈은 다음 층으로 넘어간다.
5. **느린 적은 몰리지 않게** — 도보 58초짜리 보스는 2번대로면 1.7초 안에 다 쏟아진다.
   하한을 웨이브의 55%로 둬서 벽이 되지 않게 한다.

### 실측 — 1웨이브 상단이 비어 있는 시간

같은 편성(화살탑 2기 + 궁수 + 영웅)으로 1웨이브를 끝까지 돌려
매 초 경로 위 개체 수를 셌다.

| | 상단이 빈 시간 | 초당 개체 수 |
|---|---|---|
| 이전 | **63%** | `111000001110000111100001110000011100001111000000000000` |
| 지금 | **6~11%** | `111111111111111111111111111111111211211211222222222000` |

### 마릿수를 늘렸으면 보상은 낮춘다

여기서 한 번 헛디뎠다. 채우기만 넣고 재보니 1-1 웨이브 2 시작 골드가
27 → **67**로 뛰었다. H2에서 조인 골드 경제가 통째로 풀린 것이다.

바꾼 것은 *어떻게 보이는가*이지 *얼마를 버는가*가 아니므로,
채우기 배율만큼 **마리당 보상을 나눈다**(`rewardMult`).
17마리가 1골드씩 주면 4마리가 3골드씩 주던 것과 같다.

| 웨이브 | 2 | 3 | 4 | 5 | 6 |
|-------|---|---|---|---|---|
| H2 기준 | 26 | 117 | 245 | 370 | 254 |
| 지금 | 27 | 132 | 271 | 400 | 272 |

### 곡선은 그대로

- 무한 첫 판(강화 없음) **11층** — 직전과 동일
- 무한 만렙 **30층** — 직전 31층, 시드 편차 범위
- 프레임 중앙값 16.7ms / p95 16.9ms

---

## v10.7 요약 — 플레이 보고 4건 (기획서 v3.7)

게임 버전 **v0.3.9**. 전부 실제 플레이에서 나온 보고다.

### ① 세이브가 판을 절반만 담고 있었다

> "강제로 종료한 뒤 다시 들어가면 골드는 있는데 타워랑 용병, 건물은 없는 경우가 있어"

재현됐다. 세이브에 **타워와 편성이 아예 없었다.**

| | 저장 전 | 재기동 후 (수정 전) |
|---|---|---|
| 골드 | 777 | 777 |
| 타워 | 5기 | **0기** |
| 편성 | 3명 | **0명** |
| 영웅 | 하단 배치 | **미배치** |
| 건물 | 4채 | 4채 |

두 가지가 겹쳐 있었다.

1. **담기지 않은 것** — `towers`(칸·종류·레벨·투자액), `team`(종류·HP),
   영웅의 배치·HP·전사 상태, 여관 손님. 전부 추가했다.
   복원할 때 타워는 격자 범위와 중복을 검사하고, 없는 종류는 버린다.
   영웅은 `_applyStartBonuses()`가 만피로 채운 **뒤에** 되돌려야 해서 한 박자 미뤘다.
2. **저장 시점** — 웨이브가 끝날 때만 썼다. 한 층이 60초인데 그 사이에 산 것은
   앱을 내리면 통째로 날아갔다. **10초 주기 자동 저장 + `visibilitychange`·`pagehide`·`blur`
   순간 강제 저장**을 넣었다 — 모바일에서 홈 버튼을 누르는 그 순간이다.

세이브 키(`dualfrontier_v8`)는 그대로 뒀다. 새 필드는 없으면 빈 값으로 읽히므로
기존 세이브가 깨지지 않는다.

### ② 전투 중 갑자기 마을로 넘어가던 것

> "수동전투중에 바닥을 계속 누르다보면 전투가 안끝낫는데 갑자기 마을화면으로 돌아가지는 버그"

원인이 둘이었다.

1. **`tap()`의 잔재** — 웨이브가 끝나 `wm.phase`가 `idle`이 되면, 전투 화면의 탭이
   `handleTownTap(x,y)`로 넘어가고 있었다. 준비 화면에 마을 조작이 얹혀 있던 시절의
   코드다. 지금은 마을이 별도 페이지라, **지난번 마을 화면에서 남은 버튼 좌표**에
   탭이 맞으면 그대로 마을로 튕겼다. 호출을 지웠다.
2. **손가락 밑에서 화면이 바뀐다** — 웨이브가 끝나면 아래 절반이 아레나에서 준비
   화면으로 통째로 교체되는데, 수동 전투 중에는 바로 그 자리를 연타하고 있다.
   새로 생긴 [🏰마을] 버튼이 손가락 밑으로 들어온다.
   배치가 바뀐 뒤 **0.45초 동안 탭을 먹지 않는다**(`lockTapsBriefly`).

덤으로 페이지가 바뀔 때 이전 화면의 버튼 좌표를 지우는 `clearStalePageUI()`를 넣었다 —
같은 종류의 사고를 구조적으로 막는다.

### ③ 튜토리얼이 안 꺼진다

> "처음 게임을 들어갔을때 튜토리얼이 계속뜨는데 전체튜토리얼 건너뛰기도 만들어주고"

v10.6에서 20장을 6장 + 쪽지 14장으로 나눴는데, **건너뛰기가 앞의 6장만 껐다.**
플레이어 입장에서는 층마다 쪽지가 계속 뜨니 튜토리얼이 안 꺼진 것과 같다.

`tut.skip()`이 `markAllTipsSeen()`까지 부르게 했고, 버튼도
**`✕ 안내 전부 끄기` / `남은 안내 N장까지`** 로 바꿔 무엇을 끄는지 적었다.
쪽지 카드에도 같은 버튼을 붙여, 플레이 도중에도 한 번에 끌 수 있다.

### ④ 영웅이 죽어도 아무 일이 없었다

> "영웅이 죽을경우 몇초뒤에 부활하는데 이건 리스크가 전혀없으니까"

맞다. 20초 뒤 **만피로** 돌아왔으니, 위험한 자리에 밀어넣고 죽으면 기다리는 쪽이
오히려 편했다. 시간 기반 부활을 **층 기반 결장**으로 바꿨다.

| | 이전 | 지금 |
|---|---|---|
| 전사 직후 | 20초 카운트다운 | 그 층은 영웅 없이 끝낸다 |
| 다음 층 | — | **결장** (`HERO_DOWN_FLOORS = 1`) |
| 복귀 | 만피 | **최대 HP의 40%** · 배치는 다시 고른다 |

두 기존 강화의 뜻도 여기에 맞췄다.

- 🔮 **불사의 영웅** (스킬 / 강화 카드 / 마법의 두루마리) → *전사해도 결장 없음*.
  쓰러진 층이 끝나면 바로 돌아온다. 결장이 생기면서 오히려 값이 올라간 강화다.
- 🕊️ **구원의 손** (영웅 전당 트랙, 8단계) → *복귀 HP +5%p/단계*. 만렙이면 40% → 80%.

`reviveTimer`·`HERO_REVIVE_TIME`·`heroReviveDur()`는 지웠다.

---

## v10.6 요약 — 각인 · 심층 · 튜토리얼 압축 (기획서 v3.6)

게임 버전 **v0.3.8**. v10.5 점검에서 "제안만 하고 남겨둔" 세 가지를 전부 구현하고,
포기 정산에 감액을 붙였다.

### 🏳 포기 정산 감액 — ×0.60

접을 길은 있어야 하지만, 접는 쪽이 늘 이득이면 아무도 마지막 층을 버티지 않는다.
`GIVE_UP_GEM_MULT = 0.6`. 서약 배율과는 따로 계산해서 결과 화면에 별도 줄로 보여주고,
일시정지 버튼에도 실제로 받게 될 (감액된) 수를 적는다.

### 👑 영웅 각인 — 직업별 액티브

영웅이 오래도록 레벨만 오르는 스탯 덩어리였다. 스킬 트리는 숫자를 키울 뿐,
누구의 영웅이든 6초마다 같은 일격을 쳤다. 각인은 캠프(🌳 스킬 › 👑 영웅)에서 고르는 하나의 길로,
**아레나 스킬을 통째로 갈아치우고** 그에 맞는 패시브를 얹는다.

| 각인 | 스킬 | 패시브 |
|-----|-----|-------|
| ⚔️ 검성 | 회전베기 — spin r80 ×2.9 · 6.5초 | 공격력 +15% · 공격속도 +10% |
| 🛡️ 수호자 | 불굴의 함성 — bulwark r96 · 9초 · 보호막 = 최대HP×32% + 도발 | HP +30% · 아군 방어 +4 |
| 🔥 술사 | 화염 폭발 — nova r110 ×3.6 · 9.5초 · 2초 둔화 | 사거리 +40% · 스킬 피해 +25% |

값을 치르지 않고 언제든 바꿀 수 있다. 층마다 다른 조합을 요구하는 게임에서
각인을 잠가두면 그냥 안 쓰게 된다. 바꾸면 편성된 영웅을 즉시 다시 만들고 세이브한다.

### 🌑 심층 — 40층부터

40층 넘어서 배율만 올리면 40층과 41층이 구분되지 않는다. 세 가지를 더했다.

1. **층 이벤트가 겹친다.** 해로운 것 하나 + 이로운 것 하나가 **항상** 붙는다.
   배율 키는 곱하고, 슬롯은 더하고, 시간은 짧은 쪽, 카드는 많은 쪽을 쓴다 —
   나중에 뽑힌 쪽이 앞의 것을 덮지 않도록 `_mergeFloorEvents`가 키별로 합친다.
2. **심층 전용 이벤트 5종** — 🌑암전(사거리 −50%) · 🌋지진(지형 2배) ·
   🩻광란(이동 +50%, 체력 −20%) · 🏺금고(골드 ×3) · 🗿유물(보석 ×5).
3. **심층 전용 변형 4종** — 배율이 아니라 **행동을 바꾼다**. 겹침 상한도 3→4로 올렸다.

| 변형 | 효과 | 요구하는 것 |
|-----|-----|-----------|
| 🌱 재생 | 상단 적이 초당 최대체력 1.5% 회복 | 단발 화력이 아니라 지속 화력 |
| 🌵 가시껍질 | 아레나 몹이 근접 피해 25%를 반사 | 원거리를 섞을 이유 |
| 🧬 분열 | 아레나 대형이 죽으면 소형 2기로 (동시 상한 안에서) | 몰아치기의 대가 |
| 💥 폭발 | 아레나 몹이 죽으며 주변 아군에 피해 | 뭉쳐 있지 않을 이유 |

**함께 고친 것**: `ENDLESS_ARENA_UNLOCK`이 몹 id를 `wolf` · `archer`로 적어두고 있었다.
실제 키는 `hound` · `darkarch`다. `BATTLE_MOB_TYPES[id] || goblin` 폴백에 걸려
**3층부터 아레나 다양성 슬롯 두 칸이 조용히 고블린을 내보내고 있었다** —
"깊이 가도 비슷하다"는 느낌의 실제 원인 중 하나다.

### 실측

만렙 편성 + 스크립트 플레이어로 다시 내려봤다.

| 층 | 1 | 5 | 10 | 15 | 20 | 25 | 30 | 결과 |
|---|---|---|----|----|----|----|----|-----|
| 기지 HP | 220 | 220 | 220 | 220 | 202 | 141 | 34 | **31층 사망** · 💎100 · 관문 3개 |

곡선은 그대로다 (직전 측정 27~31층). 심층 내용은 **40층부터**이므로
이 봇은 아직 닿지 않는다 — 의도한 대로 끝판 콘텐츠다.
봇은 연구를 한 번도 안 사고 케이브도 3에서 멈추므로 사람의 하한선쯤으로 읽으면 된다.

프레임은 중앙값 16.7ms / p95 17.1ms로 그대로.

### 튜토리얼 20단계 → 6단계 + 상황별 쪽지 14장

게임을 처음 켠 사람이 아무것도 만져보기 전에 20장을 넘겨야 했고,
그중 절반은 열 층은 내려가야 만나는 이야기였다. 둘로 갈랐다.

- **`TUTORIAL_STEPS` 6장** — "이 게임이 무엇인가"만. 개요 · 캠프 · 무한이 본편 ·
  준비는 마을에서 · 아래쪽은 실시간 · 출발.
- **`TUTORIAL_TIPS` 14장** — 그 일이 처음 벌어질 때 한 번씩. 비행이 처음 날아올 때
  비행 설명을 읽는 편이 시작 화면에서 읽는 것보다 낫다.

쪽지는 `tut.showTip(key)`로 뜬다. 이미 본 것(localStorage `df_tip_*`)이거나
다른 쪽지가 떠 있으면 무시하고 **표시를 남기지 않아** 다음 기회에 다시 뜬다.
쪽지가 떠 있는 동안은 시뮬레이션이 멈춘다 — 웨이브 한복판에서도 뜨기 때문이다.
본 튜토리얼에는 **◀ 이전 / 건너뛰기 ✕ / n/N** 을 카드 바깥에 붙였다
(본문 탭 = 다음과 겹치지 않게).

---

## v10.5 요약 — 전체 점검 (기획서 v3.5)

게임 버전 **v0.3.7**. 새 기능을 붙이기 전에, 지금까지 만든 것을 화면 단위로 훑었다.
헤드리스 Chromium으로 로비 5탭 · 마을 3탭 · 건물 상세 5종(1레벨/만렙) · 층 6종 ·
전투 구동 · 결과 화면을 차례로 그리며 프레임 오류를 모았다.

### 찾은 것과 고친 것

| 증상 | 원인 | 조치 |
|-----|-----|-----|
| 영웅 상점 화면이 그려지다 죽음 | `HERO_SHOP_FIXED`가 트랙 개편(v10.4) 때 같이 지워짐 | 상수 복원 |
| 케이브 업그레이드 버튼이 안 눌림 | 버튼이 케이브 카드 안에 있는데, `handleTownTap`이 카드 루프에서 먼저 `return` | 버튼 판정을 카드 루프보다 앞으로 |
| 작은 버튼이 잘 안 눌림 | 표시 배율 0.858 — 캔버스 22px 버튼이 화면에서 19px (권장 44px) | `hitTest`에 4px 여유. 작은 사각형에만 적용해 목록 행끼리 겹치지 않게 |
| 판을 중간에 접을 수 없음 | 한 판이 10~30분인데 초반에 망해도 끝까지 봐야 했다 | 일시정지 화면에 [포기하고 정산] (2단 확인) |
| 기록이 사라질 수 있음 | 세이브가 localStorage에만 있어 캐시를 지우면 끝 | 백업 코드 내보내기/복원 |

프레임은 30타워 + 아레나 가동 상태에서 중앙값 16.7ms / p95 16.9ms — 60fps 여유가 있다.

### 세이브 백업

무한 최고 층이 이 게임의 점수판인데, 그 점수판이 브라우저 캐시 하나에 매달려 있었다.
기록 탭 하단에 두 개의 버튼을 뒀다.

- **백업 코드 복사** — 지금 상태를 저장한 뒤 `DF1.<검사값>.<base64>` 한 줄로 클립보드에 복사.
  클립보드가 막힌 환경(비 HTTPS 등)에서는 `prompt`로 코드를 직접 띄운다.
- **코드로 복원** — 붙여넣기 → 태그·djb2 검사값·JSON 파싱을 차례로 확인하고 통과할 때만 덮어쓴다.
  오타 난 코드로 세이브를 깨뜨리지 않는다. 복원 뒤에는 페이지를 다시 켠다 —
  메모리에 흩어진 전역을 하나하나 되돌리는 것보다 확실하다.

### 포기하고 정산

일시정지 오버레이가 "P 키로 재개" 안내만 있던 화면에서, 재개 / 포기 두 버튼짜리로 바뀌었다.
포기는 되돌릴 수 없으므로 두 번 눌러야 하고, 버튼에 지금 받게 될 보석 수를 미리 적어 둔다.
여기까지 번 보석은 그대로 정산된다 — 망한 판을 붙들고 있을 이유를 없앤다.

---

## v10.4 요약 — 마을 재설계 (기획서 v3.4)

게임 버전 **v0.3.4**. 보고된 두 가지: 병영 Lv.2에서 목록이 잘림 / 건물이 2레벨까지뿐이라
무한 모드에서 살 것이 바닥남.

### 강화를 "트랙"으로 — 건물 10레벨

`levels[]` 하드코딩을 버리고 건물마다 **강화 트랙** 목록을 둔다. 트랙은 반복 구매한다.

```
n번째 비용 = cost × costMult^(n-1)
n번째 효과 = step × (1 + (n-1) × growth)     ← 상승치도 살수록 커진다
```

건물 레벨(1~10)은 어떤 트랙이 열리는지를 정한다. 승급 비용은 레벨당 1.7배.

| 건물 | Lv.2 | Lv.5 | Lv.8 | Lv.10 | 트랙 |
|---|---:|---:|---:|---:|---|
| ⚒️ 무기 공방 | 35 | 172 | 845 | 2,442 | 연마·조준경·속사·대량생산·강화탄두·♾️정밀세공 |
| ⚔️ 병영 | 30 | 147 | 724 | 2,093 | 전투·체력·철갑·속공·할인·급소·증원·♾️불굴의 대열 |
| 🏪 영웅 상점 | 32 | 157 | 772 | 2,232 | 단련·교본·각성·오라·구원·♾️전설의 무구 |
| 🏨 여관 | 40 | 203 | 1,036 | 3,064 | 잠자리·식사·물약·주점·별관·대접·♾️끝없는 환대 |

### ♾️ 무한 강화

Lv.10에 닿으면 상한 없는 트랙이 열린다. 비용이 지수로 계속 올라 수입이 얼마든 흡수한다.

| 회차 | 1 | 10 | 20 | 30 | 40 |
|---|---:|---:|---:|---:|---:|
| ♾️ 정밀 세공 비용 | 180 | 1,441 | 14,532 | 146,561 | 1,478,168 |

### 화면이 잘리던 이유

건물 상세 화면이 **하단 전투 영역(y 410~800)에만** 그려지고 있었다 — 위 300px이 비어 있는 채로
목록만 좁은 칸에 밀렸다. 마을 탭 아래 **전체(y 92~800)** 를 쓰도록 고쳤고,
넘칠 때를 대비해 드래그·휠 스크롤을 넣었다(6px 임계값으로 탭과 구분).

### 🏨 여관 손님 — 특수 용병 재설계

상시 고용이면 그냥 비싼 일반 용병이다. **웨이브마다 확률로 등장**하고 그 웨이브에만 고용된다.

| 여관 | 자리 | 자리당 확률 | 평균 등장 | 아무도 안 옴 |
|---:|---:|---:|---:|---:|
| Lv.1 | 1 | 28% | 0.28명 | 72% |
| Lv.3 | 1 | 38% | 0.38명 | 62% |
| Lv.6 | 2 | 53% | 1.05명 | 22% |
| **Lv.10** | 4 | 73% | 2.62명 | 1% |

🍺 소문난 주점이 확률을 최대 +61% 더 올린다. 고용하면 그 자리는 사라진다(같은 웨이브 중복 불가).

**전용 슬롯** — 일반 편성과 별개. 기본 1칸, 🚪 별관 증축으로 최대 4칸.
실측: 일반 4 + 특수 3 = 총 7명 편성.

### 실측

난이도 변화 없음 — 도달 11층, 보유 골드는 층마다 130~140에서 평평.
신규 판 10층 시점에 병영 Lv.6 · 공방 Lv.3 · 여관 Lv.1.

---

## v10.3 요약 — 흐름 정리 · 여관 (기획서 v3.3)

플레이 피드백 반영. 게임 버전 **v0.3.3**.

### 준비와 전투를 갈라놓기

| 화면 | 할 수 있는 것 | 할 수 없는 것 |
|---|---|---|
| 🏰 마을 | 타워 배치·강화·철거 · 병력 고용 · 영웅 배치 · 💰현상수배 · 건물 | — |
| 웨이브 시작 화면 | 적 구성·변형·이벤트 확인 · 타워를 탭해 **성능만** 보기 | 배치 · 강화 · 철거 · 고용 · 현상수배 |

- 준비 화면 빈 칸 탭 → `타워 배치는 🏰마을에서`
- 준비 화면 타워 탭 → `Lv.3 · ⚔12 · ◎159` (침묵 중이면 🔒)
- 현상수배 버튼을 준비 화면에서 **마을 › 출전준비**로 이동

### 👑 영웅 배치 필수

상단·하단 어느 쪽이든 배치하지 않으면 웨이브가 시작되지 않는다.
두 화면의 시작 버튼이 모두 `👑 영웅을 배치하세요`로 바뀌고 비활성화된다.
전사 후 부활 중일 때만 예외. 공통 검사는 `canStartWave()` 하나로 모았다.

### 🏨 여관 (신규 건물)

**지은 것만으로 효과가 있다** — 업그레이드를 사야 쓸모가 생기면 "짓는 결정"에 보상이 없다.

| 레벨 | 비용 | 기본 회복 | 개방 용병 |
|---|---:|---|---|
| 건설 | 35💰 | 웨이브 후 회복 **+12%** | 🗡️ 도적 |
| Lv.2 | 45💰 | 누적 **+18%** | ✝️ 성기사 |
| Lv.3 | 90💰 | 누적 **+24%** | 🎯 명사수 |

기본 휴식 30%에 얹힌다. Lv.3 + 강화까지 사면 웨이브마다 최대 HP의 절반 이상이 회복된다.
Lv.3 강화 '영웅 대접'은 웨이브 후 영웅을 완전 회복시킨다.

#### 특수 용병 3종

캠프 보석이 아니라 **런 안의 여관 레벨**로 열린다 — 골드 배분의 선택지이자 후반 사용처.

| 용병 | 비용 | HP | ATK | 사거리 | 성격 |
|---|---:|---:|---:|---:|---|
| 🗡️ 도적 | 20 | 88 | 19 | 28 | 주기 0.60초 — 가장 빠른 근접 DPS, 물몸 |
| ✝️ 성기사 | 28 | 265 | 13 | 26 | 방패병(210/9)보다 단단, 보호막 38 |
| 🎯 명사수 | 32 | 78 | 17 | 180 | 아레나 절반을 덮는 사거리 |

편성 화면에서 ★ 표시와 굵은 테두리로 구분. 다음 개방 대상을 안내한다.

### 타이틀 화면

- 버전을 하단 중앙에 크게 표기 (`v0.3.3`)
- 시작 버튼 아래 **데이터 초기화** — 두 번 눌러야 실행, 5초 후 자동 해제
- 세이브 · 보석 · 스킬 · 해금 · 기록 · 튜토리얼이 전부 지워지고 첫 실행 상태로

---

## v10.2 요약 — 층 이벤트 · 경제 재조정 (기획서 v3.2)

### 층 이벤트 11종

변형(affix)이 적의 숫자를 바꾼다면, 이벤트는 **그 층 동안의 규칙**을 바꾼다.
불리한 것만 넣으면 난이도 곡선이 한 겹 더 생길 뿐이라 유리·교환을 섞었다.

| 성격 | 이벤트 | 효과 | 가중치 |
|---|---|---|---:|
| 불리 | 🌫 짙은 안개 | 타워 사거리 −30% | 12 |
| 불리 | 🔒 봉인 | 타워 한 종류가 침묵 (무작위) | 10 |
| 불리 | 🧪 부식 | 타워 공격력 −25% | 10 |
| 불리 | 🩸 출혈 | 완주 수리·성벽 보수 없음 | 9 |
| 유리 | 💰 노다지 | 골드 ×2 | 9 |
| 유리 | ⚡ 각성 | 과부하 쿨다운 −70% (20초→6초) | 9 |
| 유리 | 👥 증원 | 편성 슬롯 +2 | 9 |
| 유리 | 🎲 풍요 | 강화 카드 5장 | 8 |
| 유리 | 💎 보물 | 이 층 보석 ×3 | 7 |
| 교환 | 🕐 속공 | 40초 · 골드 ×1.6 | 8 |
| 교환 | 🪙 탐욕 | 골드 ×2.5 · 적 HP +40% | 8 |

4층부터, 층의 약 55%. 층 번호 + 런 시드로 결정. 훈련에는 없다.

### 골드 경제 재조정

**보고: 1-4까지 진행한 판에서 8,000골드가 남고 살 것이 없었다.**
원인은 후반 사용처가 아니라 초반부터의 과잉 수급이었다.

| 웨이브 | 1 | 2 | 3 | 4 | 5 | 6 |
|---|---:|---:|---:|---:|---:|---:|
| 이전 시작 골드 | 10 | 45 | **427** | 737 | 1,307 | 1,595 |
| 지금 | 10 | **26** | 117 | 245 | 370 | 254 |

목표: **웨이브 1을 마치면 타워 두 기(10) + 용병 한 명(6)을 살 정도.** 지금 26으로 딱 그만큼.

| 항목 | 이전 | 지금 |
|---|---|---|
| 몹 보상 | 고블린 8 · 오크 20 · 오우거 38 · 마왕 180 | 3 · 8 · 15 · 70 |
| 처치 보너스 | `처치수 × (웨이브+1)` | `처치수 × (1 + 웨이브×0.12)` |
| 승리 보너스 | 20 + 웨이브×15 | 8 + 웨이브×4 |
| 완주 보너스 | 30 + 웨이브×12 | 12 + 웨이브×4 |
| 성벽 보수 | 70 ×1.6ⁿ | 40 ×1.45ⁿ |
| 강화 리롤 | 40 ×1.8ⁿ | 22 ×1.6ⁿ |
| 병기 연구 | 120 ×1.45ⁿ | 60 ×1.38ⁿ |

처치 보너스가 가장 컸다 — 마릿수가 이미 웨이브에 비례해 느는데 단가까지 곱해 이중으로 폭발했다.
사용처 비용도 함께 내렸다: 수입만 깎으면 후반 사용처에 영영 못 닿는다.

### 실측

| 측정 | 이전 | 지금 |
|---|---:|---:|
| 훈련 웨이브 2 시작 골드 | 45 | **26** |
| 훈련 최대 보유 골드 | 1,595 | **370** |
| 만렙 무한 층별 보유 골드 | 305 → 1,657 → 2,816 | **155 → 154 → 157** |
| 신규 도달 층 | 12 | 11 |
| 만렙 도달 층 | 26 | 23 |

만렙 판의 보유 골드가 깊이와 무관하게 155 언저리에서 **평평하다** — 버는 족족 쓴다는 뜻.
훈련 첫 판 기지 HP는 100 → 79 → 72 → 60으로 웨이브마다 깎인다.
완벽히 막는 것이 아니라 모자란 채로 버티다 보석으로 메우는 것이 첫 경험이다.

---

## v10.1 요약 — 다양성 (기획서 v3.1)

층 생성만으로는 부족했다. 층 번호로만 결정하면 같은 27층은 영원히 같은 27층이고,
아레나는 빈 직사각형이라 어디서 싸우든 똑같고, 경로가 하나뿐이라 최적 배치도 하나뿐이었다.

| 넣은 것 | 무엇이 달라지나 |
|---|---|
| **런 시드** | 같은 층도 판마다 적 구성·변형이 달라진다. 난이도 곡선은 시드를 타지 않는다 |
| **아레나 지형 3종** | 🪨바위(이동·화살 차단) 🌿수렁(−52% 이동) 🔺가시(초당 최대HP 1.4%) |
| **경로 변형 4종** | 관문(10층)마다 ∞ 경로가 통째로 바뀐다 — 최적 배치가 리셋된다 |
| 세이브 | `runSeed` 추가 |

### 경로가 바뀌면 기존 타워는

**부수지 않는다.** Lv.5까지 올린 타워를 예고 없이 잃으면 통제 못 하는 일로 벌을 받는 꼴이다.

1. **관문에서만 바뀐다** — 아무 때나 바뀌면 배치 계획이 성립하지 않는다
2. **한 층 전에 예고** — 격자에 점선, 새로 경로가 될 칸을 하이라이트. 겹치는 칸에 지으면 안 옮겨진다
3. **인접 빈 칸으로 자동 이설** — 레벨·투자금 유지. 자리가 없을 때만 전액 환불

이설된 타워는 6초간 파란 테두리로 표시된다.

### 실측

24개 층을 실제로 통과하며 경로가 두 번(11층 v0→v1, 21층 v1→v3) 바뀐 결과:

| 층 | 경로 | 타워 | 누적 투자 | 경로 위 타워 | 칸 중복 | 지형 |
|---:|---:|---:|---:|---:|---|---:|
| 1 | v0 | 30 | 15,000 | 0 | 없음 | 0 |
| 10 | v0 | 30 | 15,000 | 0 | 없음 | 2 |
| **11** | **v1** | 30 | 15,000 | 0 | 없음 | 2 |
| 20 | v1 | 30 | 15,000 | 0 | 없음 | 4 |
| **21** | **v3** | 30 | 15,000 | 0 | 없음 | 4 |

- 자산 손실 0. 격자를 가득 채운 34기로도 10기 이설 · **환불 0기**
  (9×7 격자는 어떤 변형에서도 35칸 이상 비므로 자리가 모자라는 일은 사실상 없다)
- **난이도 변화 없음** — 신규 11 → 12층, 만렙 27 → 26층. 셋 다 다양성만 더한다

### 경로 변형

| 변형 | 모양 | 지을 수 있는 칸 | 성격 |
|---|---|---:|---|
| 0 기본 ∞ | 8자 | 39 | 1~10층 고정 — 배우는 구간 |
| 1 넓은 ∞ | 판 바깥을 크게 | 35 | 안쪽이 비어 사거리 짧은 타워가 불리 |
| 2 뱀 | S자로 세 번 훑음 | 37 | 세로줄 하나에 몰아 지으면 세 번 때린다 |
| 3 나선 | 바깥 한 바퀴 후 안으로 | 36 | 중앙 타워가 강해진다 |

순서는 런 시드가 정한 순열. 연속으로 같은 경로는 나오지 않는다
(시드 222 예: v0 → v3 → v2 → v1 → v0 → v3).

---

## v10.0 요약 — 무한이 본편 (기획서 v3.0)

**구조 전환.** v9.1까지 이 게임은 30웨이브 캠페인 + 연장전이었다. 이제 뒤집혔다 —
**무한이 본편이고, 30웨이브는 연습이다.**

### 왜

만렙 편성이 ∞-19까지 가는 데 통산 **49웨이브**였는데, 그중 앞의 30웨이브는
결과가 이미 정해진 구간이었다. 매 판 30분(3배속 10분)을 알던 길로 걸어야
진짜 게임이 시작됐다. 웹게임에서 본편 진입에 고정 비용 30분이 붙으면
"짧게 자주"라는 강점이 사라진다.

| | 이전 | 지금 |
|---|---|---|
| 본편 | 30웨이브 캠페인 | **∞ 무한 — 1층부터, 끝이 없다** |
| 30웨이브 | 본편 | **훈련 — 한 판 치르면 무한이 열린다** |
| 무한 진입 | 30웨이브 완주 필요 | 1층부터 독립 곡선 (진입 비용 0) |
| 층 정의 | 마지막 3웨이브 재활용 | **층 번호에서 생성** |
| 기록 | 최고 웨이브 | **최고 층 · 돌파한 관문** |
| 정산 | 도달 웨이브 × 0.5 | 층당 적립, 깊이에 비례해 몫이 커진다 |
| 세이브 | v7 | **v8** (`mode` · `clearedGates` · `bestEndless`) |

해금 조건은 완주가 아니라 **한 판 치르기**다. 완주를 걸면 신규 플레이어는 열지 못한다 —
첫 런 도달이 11웨이브인데 훈련은 30웨이브라, 본편에 들어가려고 연습을 수십 번
반복하는 꼴이 된다. 그건 이 개편이 없애려던 바로 그 낭비다.

### 층 생성 알고리즘

```
배율(t) = (1 + (t−1) × 0.09) × 지수^(t−1)
```

| 축 | 지수 | 10층 | 20층 | 30층 |
|---|---:|---:|---:|---:|
| 상단 적 HP | 1.055 | ×2.9 | ×7.5 | ×17.1 |
| 아레나 몹 | 1.040 | ×2.6 | ×5.7 | ×11.3 |
| 상단 물량 밀도 | 0.975 | ×0.80 | ×0.62 | ×0.48 |
| 상단 이동속도 | 1.022 | ×1.22 | ×1.51 | ×1.88 |
| 기지 피해 | 1.035 | ×1.36 | ×1.92 | ×2.71 |

- **적 합류 순서** — 상단: 고블린(1) 늑대(3) 오크(5) 박쥐(8) 강철오크(11) 비룡(15) 던전보스(19).
  가중치 `max(0.22, 1.55 − 나이×0.035)` — 오래된 적도 0이 되지 않는다.
- **층 변형 7종** — 🕊창공 🛡강철 💨폭주 🐝무리 💀정예 🗿거인 🌊해일.
  5층부터 1개, 15층부터 2개, 30층부터 3개. **결정적**(씨앗 = 층 번호)이라 같은 27층은 언제나 같다.
- **🏁 관문** — 10층마다. 물량 −28%, 대형 비중 2배, 체력 +30%. 최초 돌파에 일회성 보석.

### 보석

```
층당 = 0.35 + (층 − 1) × 0.055
```

정액이면 20층 → 40층의 열 배 어려운 구간이 두 배 값어치밖에 안 된다.

| 도달 층 | 10 | 20 | 30 | 40 | 50 |
|---|---:|---:|---:|---:|---:|
| 누적 | 6 | 17 | 34 | 57 | 85 |
| 관문 보너스 누적 | +10 | +24 | +42 | +64 | +90 |

### 실측 — 세 단계 모두 죽는다

| 단계 | 스킬·해금 | 도달 층 | 보석 | 관문 | 한 판(3배속) |
|---|---|---:|---:|---:|---:|
| 신규 | 0 · 0 | **11층** | 17💎 | 1 | 약 4분 |
| 중간 | 14 · 3 | **20층** | 43💎 | 1 | 약 7분 |
| 만렙 | 27 · 7 | **27층** | 67💎 | 2 | 약 9분 |

성장이 곧 도달 층으로 환산된다(11 → 20 → 27). v9.1까지 만렙은 죽는 지점이 아예 없었다.

**이 숫자는 천장이 아니라 바닥이다.** 드라이버는 강화를 무작위로 고르고, 타워를
기계적으로 놓고, 아레나를 수동 조작하지 않고, 현상수배를 부르지 않는다.
그 여지가 기록 경쟁의 공간이다.

---

## v9.1 요약 — 엔드게임 (기획서 v2.6)

만렙 편성이 죽지 않던 문제를 계측으로 원인부터 다시 잡았다.

| 바뀐 것 | 이전 | 지금 |
|---|---|---|
| 30웨이브 이후 | 없음 (정산하고 끝) | **∞ 무한 모드** — 갈림길에서 선택, 죽어야 끝난다 |
| 잔존 침입자 | 웨이브 끝나면 전부 소멸 | **다음 웨이브로 이월** (경로 위치 유지, 상한 40기) |
| 기지 피해 감소 | 상한 없음 (실측 0.65~0.85) | **0.55 상한** |
| 돌파·후퇴 상한 | 상수 (1.8/s · 14) | 무한 층에 비례 (층당 ×1.03, 상한 2.5배) |
| 후퇴 비용 | 기지 피해 감소 적용 | **미적용** — 자발적 이탈은 적의 공격이 아니다 |
| 서약 | 6종 가산형 | **10종 3계열 배율형** (전부 걸면 보석 ×2.84) |
| 골드 사용처 | 성벽 보수 · 리롤 · 타워 Lv.4~5 (전부 상한 있음) | **+ 병기 연구** (120부터 ×1.45, 상한 없음) |

### 근본 원인 — 적이 "시계에 죽고" 있었다

∞ 경로는 1,236px이고 웨이브는 60초다.

| 적 | 이동 | 경로 완주 | 결과 |
|---|---:|---:|---|
| 고블린 | 58.3px/s | 21.2초 | 도착한다 |
| 오크 | 37.1px/s | 33.3초 | 도착한다 |
| 강철오크 | 26.5px/s | 46.6초 | 중반 이후 스폰분은 못 온다 |
| 던전보스 | 21.2px/s | 58.3초 | **사실상 절대 못 온다** |

적 HP를 ×44.7까지 올려도 **기지 도달 0기**였고, 한 웨이브 손실이 층과 무관하게
**정확히 9HP**로 고정돼 있었다(= 돌파 상한 1.8/s × 0.35 × 15초). 체력을 아무리 올려도
상단이 위협이 될 수 없는 구조였다.

### 무한 층 성장률

| 축 | 층당 | ∞-10 | ∞-20 |
|---|---:|---:|---:|
| 상단 적 HP | ×1.17 | ×4.8 | ×23.1 |
| 상단 물량 밀도 | ×0.96 (하한 0.40) | ×0.66 | ×0.44 |
| 상단 이동속도 | ×1.035 (상한 2.4) | ×1.41 | ×1.99 |
| 아레나 몹 | ×1.085 | ×2.3 | ×5.2 |
| 돌파·후퇴 상한 | ×1.03 (상한 2.5) | ×1.34 | ×1.81 |
| 층당 보석 | +0.7 | +7 | +14 |

상단을 아레나보다 가파르게 잡은 이유: 같은 지수를 먹였더니 아레나가 8층 먼저 무너져
**상단 도달 0기로 끝나는** 무한 모드가 됐다.

### 실측 (만렙: 스킬 27 · 해금 7 · 타워 34기 Lv.5 · 연구 12 · 케이브 최대 · 영웅 Lv.10)

| 층 | 적 HP | 기지 HP | 손실 | 잔존 |
|---:|---:|---:|---:|---:|
| ∞-1 | ×1.2 | 220 → 209 | −11 | 0기 |
| ∞-10 | ×4.8 | 217 → 202 | −15 | 0기 |
| ∞-15 | ×10.5 | 170 → 153 | −17 | 3기 |
| **∞-18** | ×16.9 | 118 → 99 | −19 | **29기** |
| ∞-19 | ×19.7 | **기지 함락** | | |

- **∞-18에서 잔존이 0 → 29기로 튄다** — 타워가 초당 도착량을 못 따라가는 지점.
- 통산 **49웨이브**에서 사망. 이전에는 죽는 지점이 아예 없었다(13웨이브 220 → 212).
- 후퇴 런과 전멸 런이 둘 다 ∞-18~21에서 끝나 **지배 전략이 없다.**
- 첫 런 도달 웨이브 **11.6**(5회 평균, 이전 9.7) — 신규 플레이어는 영향 없음.

### 병기 연구의 무게 배분

타워 +4 / 아군 +3으로 잡았더니 연구 12단계가 타워 총 DPS를 **32,821 → 56,589(+72%)**로
올렸다 — 남는 골드를 흡수하라고 연 창구가 상단을 다시 무적으로 만들었다.
실제로 무너지는 쪽은 아레나이므로 **타워 +2 / 아군 +4**로 뒤집었다.

---

## v9.0 요약

| 바뀐 것 | 이전 | 지금 |
|---|---|---|
| 하단 전투 | 1초 1틱 턴제 · 그룹 순환 | **실시간 아레나 480×330** — 가장자리 랜덤 리젠 · 자동 타게팅 |
| 조작 | 후퇴 버튼 하나 | **자동/수동 전환**(A) · 아레나 탭/방향키로 부대 이동 |
| 게임 진입 | 타이틀 → 즉시 웨이브 1 | 타이틀 → **캠프(로비)** → 출격 → 결과 → 캠프 |
| 보석 사용 | 죽어야만 열리는 스킬 트리 | 캠프에서 언제든 — **스킬 + 해금 + 서약** |
| 해금 | 없음 (전부 개방) | 타워 3종 · 병력 3종을 보석으로 영구 해금 (총 52) |
| 신규 모듈 | — | `arena.js` · `formation.js` · `lobby.js` |
| 세이브 | v6 | **v7** (`unlocked` · `pacts` · `seenMobs` 추가) |
| 웨이브 종료 | 후퇴는 공짜 | **완주 · 후퇴 · 전멸 3단계** — 아래 참조 |

### 웨이브를 끝내는 세 가지 방법 (v9.0)

| 결과 | 조건 | 골드 | 기지 | 병력 |
|---|---|---|---|---|
| ★ 완주 | 60초를 버텼다 | 승리 보너스 + 완주 보너스 `30 + 웨이브×12` | **수리 +2~6HP** | 유지 |
| 🛡 후퇴 | 스스로 하단을 비웠다 | 승리 보너스 절반 · 바닥 드랍 50%만 | **−남은 초 × 0.20** (상한 14) | 유지 |
| ☠️ 전멸 | 병력을 모두 잃었다 | 없음 | 돌파 최대 −27HP | 전멸 |

후퇴 비용은 후퇴 버튼에 실시간으로 뜬다(`🛡 후퇴 -9`). 일찍 뺄수록 비싸다.

### 후반 골드 사용처 (v9.0)

발전한 편성에서 웨이브 5부터 2,000~3,600골드가 놀던 문제를 잡는다.
셋 다 반복 구매 가능하고 살수록 비싸진다.

| 사용처 | 얻는 것 | 비용 |
|---|---|---|
| 🧱 성벽 보수 (마을) | 기지 HP +12 | 70부터 ×1.6씩 |
| 🎲 강화 카드 리롤 (픽 화면) | 3장 다시 뽑기 | 40부터 ×1.8씩 |
| 🗼 타워 Lv.4~5 | 화살탑 기준 피해 5.4배 | Lv.3 위로 급격히 상승 |

보유 골드 정점 3,617 → 1,501, 중반 이후 40대 유지. 도달 웨이브는 12.7로 불변.

### 상단 전선 개편 (v9.0)

격자가 차면 상단이 완전히 자동이 되던 문제를 잡는다.

**① 등급 상성** — 적은 🔹소형/🔸중형/🔶대형/🔺비행. 타워마다 배율이 다르다.

| 타워 | 소형 | 중형 | 대형 | 비행 |
|---|---|---|---|---|
| 🏹 화살탑 | 1.25 | 1.00 | 0.60 | 0.75 |
| ❄️ 서리탑 | 1.10 | 1.25 | 0.70 | 0.50 |
| 💣 대포탑 | 0.65 | 1.10 | 1.50 | 0.30 |
| 🎯 저격탑 | 0.70 | 1.05 | 1.45 | 1.20 |
| ⚡ 번개탑 (신규) | 1.40 | 0.90 | 0.55 | 1.60 |

**② 비행 항로** — 박쥐·비룡은 ∞ 경로가 아니라 판 바깥을 도는 별도 항로(603px)로 온다.
경로에만 지은 배치는 하늘을 놓친다. 웨이브 10부터 등장.

**③ 웨이브 중 개입** — 타워 탭 = 5초 공속 ×3 (쿨 20초), 빈 칸 탭 = 영웅 이동.

**④ 💰 현상수배** — 준비 화면에서 직접 부르는 강한 적. 잡으면 보석, 놓치면 큰 피해.
스테이지당 1회, 부를수록 강해지고 보상 증가 (HP ×1.85씩, 보석 1→1→2→2→3…).

실측(웨이브 12, 상단만 분리): 경로에만 8기 → HP 50 / 같은 자리 +3기 → HP 50 /
하늘길에 번개탑 3기 → HP 100. **물량이 아니라 배치가 답이다.**

---

## 1. 핵심 컨셉

| 항목 | 내용 |
|------|------|
| 장르 | 타워 디펜스 + 실시간 액션 로그라이트 |
| 플랫폼 | 모바일 웹 (세로형 480×800, 캔버스 단일 렌더) |
| 핵심 철학 | **두 전선을 다른 방식으로 굴린다** — 위는 배치해두고 지켜보고, 아래는 직접 뛰어들어 벌어온다 |

### 성장 3축
```
① 인게임 골드    타워 종류/레벨 · 병력 · 마을 건물 · 케이브
   (매 웨이브)    → 단기 전술

② 강화 픽        웨이브 클리어마다 랜덤 3장 중 1장
   (웨이브마다)   → 이번 판의 빌드 방향

③ 스킬 트리      런 종료 시 얻은 보석으로 영구 해금 (타워/영웅/보조 3계열)
   (런마다)       → 실패해도 강해지는 구조
```

---

## 2. 화면 구성

```
┌──────────────────────────────┐
│  방어 구역   0 ~ 355         │  9×7 격자 · ∞ 경로 · 타워/영웅
├──────────────────────────────┤
│  정보 바   355 ~ 410         │  스테이지 · 타이머 · 골드 · 마을 버튼
├──────────────────────────────┤
│  전투 구역 410 ~ 800         │  출전 브리핑 ↔ 사이드뷰 자동전투
└──────────────────────────────┘

[마을 페이지] — 기지(🏰) 탭 또는 T 키
  🏰 마을 탭    건물 건설/레벨업 (무기공방·병영·영웅상점·케이브)
  ⚔️ 출전준비   영웅 배치 · 병력 5종 고용 · 전투력 요약
  🏹 타워배치   타워 4종 팔레트 · 미니 그리드 · 배치 현황
```

---

## 3. 게임 루프

```
[타이틀]
    ↓
[출전 브리핑]  이번 웨이브 그룹 편성 · 상단 침입자 · 내 편성 확인
    ↓ (마을에서 준비)
[웨이브 60초]  상단 ∞ 경로 디펜스  +  하단 그룹 순차 전투 (동시 진행)
    ↓
  ┌── 기지 HP 0 ──→ [게임 오버] → 보석 → 스킬 트리
  └── 웨이브 클리어 ─→ [강화 픽 3장 중 1장] → 다음 웨이브
                              ↓ (3웨이브마다)
                        [스테이지 클리어 · 보석 지급]
                              ↓ (30웨이브)
                        [전체 클리어]
```

---

## 4. 상단 — 타워 디펜스

### 4-1. 타워 4종

| 타워 | 기본비용 | 공격력 | 공속 | 사거리 | 특성 |
|------|--------|-------|------|-------|------|
| 🏹 화살탑 | 5 | 2 | 1.0/s | 2.4칸 | 저렴한 단일 연사 |
| ❄️ 서리탑 | 12 | 1 | 0.9/s | 2.2칸 | 적중 시 이동속도 45% 감소 (1.6초) |
| 💣 대포탑 | 18 | 8 | 0.5/s | 2.0칸 | 착탄 반경 40px 범위 피해 (50%) |
| 🎯 저격탑 | 26 | 18 | 0.35/s | 5.0칸 | 방어 관통 · 체력이 가장 높은 적 우선 |

- **레벨 1~3**: 강화 비용 = 기본비용 × 0.9 × 현재레벨
  - Lv2 = 공격 ×1.7 / 공속 ×1.15 / 사거리 ×1.12
  - Lv3 = 공격 ×2.6 / 공속 ×1.30 / 사거리 ×1.25
- **판매**: 투자 골드의 60% 환급
- **건설비 체증**: 같은 종류 1기당 +28% — 한 종류 도배를 억제
- 타워 스탯은 매 발사마다 현재 강화를 반영해 계산 (건설 후 산 강화도 적용)

### 4-2. 침입자 5종

| 적 | HP | 속도 | 기지 피해 | 골드 | 방어력 |
|----|----|----|---------|-----|-------|
| 🟢 고블린 | 14 | 1.10 | 2 | 3 | 0 |
| 🐺 늑대 | 12 | 2.30 | 2 | 5 | 0 |
| 👾 오크 | 40 | 0.70 | 6 | 8 | 1 |
| 🛡 강철오크 | 95 | 0.50 | 12 | 18 | 3 |
| 💀 던전보스 | 400 | 0.40 | 20 | 50 | 5 |

- **방어력**: 투사체 피해에서 차감 (저격탑은 무시). 최소 피해 1 보장
- **웨이브 스케일링**: HP ×(1 + 웨이브×0.22) · 물량 ×(1 + 웨이브×0.07) · 5웨이브마다 방어력 +1
- 상단 처치도 **골드를 준다** (v8.0 이전에는 보상이 0이었음)

### 4-3. 영웅 (상단 배치)
- 사거리 내 적을 1초마다 자동 공격, 처치 시 EXP 전액
- 타워가 잡은 적에게서도 EXP 40% 획득
- 근접한 적(0.75칸 이내)에게 **반격당하며 전사할 수 있다** → 20초 부활 대기

---

## 5. 하단 — 그룹 순차 자동전투

### 5-1. 아군 유닛 5종

| 유닛 | 비용 | HP | ATK | DEF | 스킬 | 효과 |
|------|-----|----|----|----|------|------|
| 🏹 궁수 | 6 | 40 | 10 | 1 | 연사 | 무작위 3연타 (12×3) |
| ⚔️ 검사 | 8 | 60 | 12 | 3 | 강타 | 단일 28 피해 |
| ✚ 치유사 | 10 | 45 | 5 | 2 | 치유 | 가장 다친 아군 25 회복 |
| 🛡️ 방패병 | 14 | 120 | 7 | 9 | 수호 방벽 | **아군 전체 보호막 +22** |
| ✨ 마법사 | 16 | 45 | 9 | 1 | 화염 폭발 | **적 전체 22 광역 피해** |

- **보호막**: HP보다 먼저 소모 (유닛 둘레 하늘색 링)
- **방패병 어그로**: 살아 있으면 적이 60% 확률로 방패병을 노린다
- 1초 1틱, 5틱마다 스킬 자동 발동
- **웨이브 종료 휴식**: 생존 병력 최대 HP의 30% 회복 + MP 완충

### 5-2. 몬스터 5종

| 몹 | HP | ATK | DEF | 골드 |
|----|----|----|----|-----|
| 👺 고블린 | 30 | 8 | 1 | 8 |
| 👹 오크 | 80 | 15 | 4 | 20 |
| 🧌 오우거 | 150 | 22 | 6 | 38 |
| 💀 보스 | 200 | 25 | 8 | 60 |
| 🐲 마왕 | 520 | 40 | 14 | 180 |

- **그룹 순차 등장**: 한 그룹을 전멸시켜야 다음 그룹이 나온다 (그룹당 최대 5마리)
- **처치 스케일링**: 다음 몹 스탯/보상 ×(1 + 처치수×0.08)
- **케이브 배율**: Lv1(×1.0) ~ Lv5(스탯 ×3.5 / 보상 ×4.5)
- **엘리트**: 케이브 '엘리트 소환' 강화로 확률 상승. 스탯 ×1.8 / 보상 ×2.5
- **전리품**: 케이브 '아이템 발굴' 강화로 처치 시 추가 골드

### 5-3. 그룹 순환 — 하단은 시간 내내 돌아간다 (v8.1)

하단은 **자원 확보 구간**이므로, 상단 방어를 일찍 막았다고 해서 끊기지 않는다.

- 웨이브의 그룹을 모두 처치하면 **처음 그룹부터 다시 순환**한다
- 웨이브는 **타이머(60초)로만** 종료된다 (양쪽 다 더 진행할 게 없을 때만 조기 종료)
- 순환할수록 처치 스케일링이 누적되어 몬스터가 계속 강해진다
- **순환 보상 체감**: `보상 배율 = 1 / (1 + 순환수 × 0.5)` → 100% / 67% / 50% / 40%…
  무한 파밍이 성립하지 않도록 막는다

v8.1 이전에는 하단 그룹이 3개 유한이라 다 잡으면 `allGroupsDone`으로 웨이브가
즉시 끝났다. 그 결과 **강한 팀일수록 웨이브가 빨리 끝나 골드를 덜 벌었다.**

| 팀 | 이전 지속/골드 | 이후 지속/골드 |
|----|------------|------------|
| 약함 | 21초 / 170 | 36초 / 202 |
| 보통 | 15초 / 495 | 42초 / 419 |
| 강함 | **13초 / 466** | **55초 / 739** |

### 5-4. 후퇴 — 언제 멈출지 고르는 결정 (v8.1)

순환이 무한하므로 가만두면 반드시 전멸한다. 자동전투에 플레이어 결정을 넣기 위해
전투 중 **🛡 후퇴** 버튼(또는 `R` 키)을 제공한다.

| | 병력 | 적립 골드 | 기지 |
|--|-----|---------|------|
| 후퇴 | 보존 | 유지 | 피해 없음 |
| 전멸 | 사망(재고용 필요) | 유지 | 돌파 피해 |

"얼마나 더 욕심낼 것인가"를 고르는 push-your-luck 구조가 된다.
후퇴해도 상단이 남아 있으면 웨이브는 계속 진행된다.

### 5-5. 돌파 — 두 전선의 연결 (v8.0 신규)

아군이 전멸하면 남은 몬스터가 **기지로 돌파해 초당 피해**를 준다.

```
초당 피해 = min(생존 몹 공격력 합 × 0.03, 2.5) × (1 - 기지 방어율)
```

돌파는 **15초 동안만** 이어지고, 그 뒤 몬스터는 물러난다. 남은 시간은 상단만
진행한다. (웨이브가 타이머까지 이어지게 바뀌면서, 제한이 없으면 전멸이 곧
게임오버가 되기 때문)

이전까지는 하단에서 전멸해도 웨이브 보너스만 놓칠 뿐 실제 패배로 이어지지
않았고, 그 결과 **기지 HP가 30웨이브 내내 한 번도 깎이지 않아 게임에서 질 수가
없었다.** 돌파 규칙으로 두 전선이 연결되어 하단 전투가 실제 위험이 되었다.

---

## 6. 스테이지 구성 (10 스테이지 × 3 웨이브 = 30)

| 스테이지 | 상단 | 하단 그룹 |
|--------|------|---------|
| 1-1 | 고블린 | 고블린 |
| 1-2 | 고블린 + 늑대 | 고블린 → 오크 |
| 1-3 | 고블린 + 오크 | 오크 → 오우거 |
| 1-4 | 강철오크 등장 | 오우거 → **보스** |
| 1-5 | 늑대·강철오크 | 보스 + 오우거 |
| 1-6 | 오크·강철오크 다수 | 보스 2마리 |
| 1-7 | 물량 증가 | **마왕** 첫 등장 |
| 1-8 | 강철오크 주력 | 마왕 + 오우거 |
| 1-9 | 던전보스 등장 | 마왕 2마리 |
| 1-10 | 던전보스 다수 | 마왕 3 + 보스 2 (최종) |

v8.0 이전에는 1-4부터 모든 그룹이 동일한 `boss`의 반복이었고, 최대 9마리
그룹은 `unitY`의 고정 64px 간격 때문에 y=913까지 밀려나 **화면 밖에 그려졌다.**
지금은 그룹당 최대 5마리, 행 간격은 아군/적 중 큰 쪽에 맞춰 자동 계산된다.

---

## 7. 강화 픽 · 마을 · 스킬 트리

### 7-1. 웨이브 클리어 강화 픽
- 웨이브마다 랜덤 3장 → 1장 선택 (Common 60 / Rare 28 / Epic 12)
- 일회성 카드(천둥 화살·불굴의 의지·불사의 영웅·철옹성·엘도라도)는 중복 출현하지 않음
- **선택 즉시 이미 고용한 병력 스탯에도 반영**

### 7-2. 마을 건물

| 건물 | 건설비 | 역할 |
|------|------|------|
| ⚒️ 무기 공방 | 40 | 타워 공격력·공속·사거리·비용 |
| ⚔️ 병영 | 30 | 아군 공격력·방어력·HP·슬롯·콤보 |
| 🏪 영웅 상점 | 25 | 소모품 + 영웅 장비 (랜덤 3종 진열) |
| 🗿 몬스터 케이브 | 0 | 골드 배율·몹 약화·엘리트·전리품 |

### 7-3. 스킬 트리 (보석)
- 🏹 타워 / 👑 영웅 / ⚙️ 보조 3계열 × 9노드, 선행 노드 필요
- 보석은 스테이지 클리어(1개, 최종 2개)와 런 종료 정산으로 획득

```
보석 = 클리어 웨이브 × 5 + 남은 기지 HP × 0.2 + 케이브 Lv × 3 + 런 처치수 × 0.3
```

---

## 8. 영웅 성장

Lv.1 → **Lv.10** (30웨이브 분량에 맞춰 확장)

| Lv | 필요 EXP | ATK | HP | DEF | 사거리 |
|----|--------|-----|----|----|-------|
| 1 | 30 | 15 | 80 | 5 | 3.0칸 |
| 3 | 130 | 22 | 105 | 7 | 3.4칸 |
| 5 | 340 | 35 | 150 | 11 | 4.0칸 |
| 7 | 720 | 55 | 215 | 16 | 4.4칸 |
| 10 | — | 105 | 360 | 26 | 5.2칸 |

---

## 9. 조작

| 조작 | 방법 |
|------|------|
| 타워 종류 선택 | 마을 › 타워배치 팔레트 / 키보드 `1`~`4` |
| 타워 건설 | 빈 격자 칸 탭 (방어 구역 또는 미니 그리드) |
| 타워 강화·판매 | 세운 타워 탭 → 패널 버튼 |
| 웨이브 시작 | 브리핑 버튼 / 출전 버튼 / `Space` |
| 하단 전투 후퇴 | 전투 화면 🛡 후퇴 버튼 / `R` |
| 마을 ↔ 전투 | 기지(🏰) 탭 / `T` |
| 일시정지 | ⏸ 버튼 / `P` |
| 배속 (1x·2x·3x) | 배속 버튼 / `S` |
| 음소거 | 🔊 버튼 / `M` |

- **사운드**: WebAudio 절차적 합성 (외부 에셋 없음, 첫 탭에서 컨텍스트 해제)
- **이펙트**: 피격·처치 파티클, 폭발 링, 기지 피격 시 화면 흔들림

---

## 10. 기술 스택

```
index.html       — 스크립트 로드 순서 (빌드 도구 없음)
js/
  constants.js   — 레이아웃, 경로, 타워/유닛/몹 스탯, 30웨이브, 영웅 테이블
  audio.js       — WebAudio 절차적 효과음 (SFX)
  fx.js          — 파티클 / 화면 흔들림 (FX)
  upgrade.js     — BONUSES, 강화 카드, 스킬 트리, 보석 계산
  town.js        — 마을 건물·업그레이드·영웅 상점
  save.js        — localStorage 세이브 (진행 + 영구 기록 + 통계)
  defense.js     — 타워 스탯/타게팅, 침입자 이동, 투사체(감속·범위·관통)
  battle.js      — 유닛 생성, 스킬 5종, 보호막, 틱 전투, 휴식 회복
  wave.js        — 웨이브 타이머, 그룹 스폰, 종료 정산, 스테이지 보상
  tutorial.js    — 튜토리얼
  render.js      — 전체 캔버스 렌더링
  game.js        — 메인 루프, 입력(탭/키보드), 상태 관리, 돌파, 런 정산
```

---

## 11. v8.0에서 수정한 기존 버그

| 문제 | 내용 | 해결 |
|------|------|------|
| 상단 처치 보상 0 | `updateProjectiles(..., ()=>{}, dt)` — 콜백이 빈 함수여서 타워가 골드·EXP를 전혀 주지 않았다 | `onDefenseKill()` 연결 |
| 보스 그룹 화면 밖 | 그룹이 9마리까지 커지는데 `unitY`는 64px 고정 간격이라 y=913(캔버스 800)까지 밀려났다 | 그룹 최대 5마리 + 행 간격 동적 계산 |
| 영웅이 죽지 않음 | `hero.dead`를 설정하는 코드가 없어 부활 로직 전체가 도달 불가능했다 | 근접 반격 + `killHero()` + 부활 카운트다운 |
| 게임에서 질 수 없음 | 하단 전멸이 아무 대가가 없어 기지 HP가 30웨이브 내내 100 유지 | 돌파 규칙 도입 |
| 무효 강화 3종 | `eliteChance` `dropChance` `spawnSpeedMult`를 쓰기만 하고 읽는 곳이 없었다 | 엘리트 몹·전리품·스폰 속도로 연결 |
| 타워 스탯 고정 | `spd`/`range`를 건설 시점에 스냅샷해 이후 강화가 반영되지 않았다 | `towerStats()`로 실시간 계산 |
| 강화 미반영 | 유닛이 고용 시점 스탯을 스냅샷해 이후 픽이 적용되지 않았다 | `refreshTeamStats()` |
| 부상 회복 불가 | 생존 병력이 영구히 다친 채로 남았다 | 웨이브 종료 시 30% 휴식 회복 |
| 화면 빈 공간 | 전투 대기 화면 ~300px, 마을 3개 탭 250~380px가 완전 공백 | 브리핑 패널 / 전투력 요약 / 배치 현황 |
| UIBar 텍스트 겹침 | 스테이지·웨이브·타이머·누적골드가 55px 안에서 서로 겹쳐 뭉개졌다 | 행 재배치 |
| 유닛 2종 도달 불가 | 고용 카드가 3종 고정 폭이라 방패병·마법사를 뽑을 수 없었다 | 그리드 배치 |
| 죽은 코드 | 마을 페이지 도입 후 호출되지 않는 렌더 함수 367줄 | 제거 |
| 하단이 조기 종료 | 그룹 3개를 다 잡으면 웨이브가 끝나, 강한 팀일수록 자원을 덜 벌었다 (13초 만에 종료) | 그룹 순환 + 타이머 종료 |

---

## 12. 밸런스 측정

헤드리스 Chromium에서 스크립트 플레이어로 30웨이브를 반복 측정했다.

**수정 전** — 기지 HP가 27웨이브 내내 100/100. 패배 불가.

**수정 후** (메타 강화 없는 첫 판, 마을 미투자)

| 스테이지 | 1-1~1-3w1 | 1-3w2 | 1-3w3 | 1-4w1 | 1-4w2 |
|--------|---------|-------|-------|-------|-------|
| 기지 HP | 100 | 88 | 70 | 40 | 14 → 0 |

골드는 전 구간 20~250 사이를 유지한다 (건설비 체증 도입 전에는 후반에 수천 단위로 폭주).

---

## 13. 향후 과제

- [x] 영웅 고유 스킬 → 👑 각인 3종 (v10.6)
- [x] 40층 이후 단조로움 → 🌑 심층 (v10.6)
- [x] 튜토리얼 압축 → 6단계 + 상황별 쪽지 (v10.6)
- [ ] 80층 이후 — 심층 풀도 언젠가 한 바퀴 돈다
- [ ] 업적 · 기록 비교
- [ ] BGM

---

*문서 버전: v8.1*
